# memo 重审 · 体验类拍板呈报

> 汇自 `.scratch/review-deep/` 下 memo 回滚后重审五方向报告（memo2-efficiency / memo2-ui / memo2-func / memo2-consistency / memo2-arch，2026-09-19/20），只收「体验 / 交互 / 口径」类拍板项，按决策主题重排。
> **使用方式：以下 20 个决策项，可逐项回复「编号+选项」（如「1A」「9B」），也可只回复有异议的项，其余按★建议执行。** 每项末尾的★是主线程给出的默认建议。
> 覆盖口径：交互效率报告标注〔拍板〕的 15 条全数入选；UI 报告 UX 4 条入选；一致性报告明确留给拍板的 1 条（场景删除确认）入选。纯功能缺陷、性能问题与已定稿范式对齐（免确认删除、脏表单拦截、入焦圈闭与悬浮隔离接线、各类单源收编等）不列本单，走本轮修复批；被砍功能（周期重复 / 月历 / 清单子任务）相关条目按口径作废。
> 括号里的编号（如 效#5、MR2-4）是原始报告编号，便于回溯；每条末尾另有一行小字备查代码位置。文末附完整对照表。

---

## 一、录入与保存（3 项）——写备忘录、存备忘录的事

### 1. 编辑弹窗里敲回车，存不了盘（效#1；四报同源 M3-11／待接-1／A4）
备忘录编辑弹窗是全插件字段最多的表单（内容、场景、优先级、截止、定位绑定、剪藏信息），可底部快录框敲回车就能保存，进了编辑弹窗反而只能伸手点「保存」——剪藏标题、脚本名、课程名这些单行框里按回车毫无反应。附件、归物、书库、影视、日记等域都已支持回车提交，备忘录是唯一没跟上的主力表单。
- **A. 接入统一的回车提交能力**——单行框回车即存，正文框 Ctrl+Enter 提交（正文换行不受影响）；与全插件键盘习惯一致，顺带清掉两个场景弹窗各自手写的回车/ESC 处理。
- **B. 维持只能点按钮**——零改动；但键盘流每次都得中途换鼠标。

★**建议 A**。

*备查：`ui.ts:1253` 保存钮仅绑点击；core 能力 `core/ui/modal.ts:27` 已就绪、七个域已接，memo 全域零消费（memo2-ui M3-11、memo2-consistency 待接-1、memo2-arch A4 同源）。*

### 2. 落盘慢半拍时连点「添加」，会存出两条一样的（效#4；M2-1／功能#1）
点「添加」到真正写完盘之间有一小段窗口（库大或同步盘上能感觉到），手快连点两下，两条相同内容的备忘录都进了库——还得手动删一条，删除又要点确认、看撤销通知，双重摩擦。底部快录框已有防连点保护，编辑弹窗漏配了同款。
- **A. 点击即锁定保存钮**，落盘完成前再点无效；与快录框行为对齐。
- **B. 维持现状**——零改动；但慢盘上双击必出重复条目。

★**建议 A**（与第 1 条同批做：回车和按钮走同一个保存入口，防连点只写一处）。

*备查：`ui.ts:1253-1338` 无防重入门控；快录侧 `composerBusy`（`ui.ts:962`）为同文件先例（memo2-ui M2-1、memo2-func #1 同源）。*

### 3. 改个截止时间，要逐段拨日期时间轮（效#3）
给条目设或改截止时间，只有逐段拨的日期时间控件，没有「今天晚上」「明早」这类一键档；而右键菜单里「延后 1 天 / 3 天」倒是一键的——改期明明是高频动作。
- **A. 时间控件旁挂「今天 18:00 / 明天 09:00」快捷档**——点一下填好；条目原有时刻则沿用（与「延后」语义一致）。日记本已有「此刻/昨天」同款先例。
- **B. 维持逐段拨**。

★**建议 A**。

*备查：`ui.ts:1176-1193` 截止字段无预设档；4-5 次击点可省成 1 次。*

---

## 二、搜索体验（4 项）——找得到、退得出的搜索

### 4. 搜索框里按 ESC 想清词，整个面板关了（效#5）
搜完想换个词，下意识按 ESC——结果是备忘录面板整个被关掉，搜索白打，重开还得重新找场景。日记本已经是「有词先清词、没词才关」。
- **A. 搜索框有词时 ESC 先清词**，没词才放行关面板，对齐日记本先例。
- **B. 维持 ESC 直接关面板**。

★**建议 A**。

*备查：`ui.ts:476-477` 搜索框未拦 Escape；按键冒泡到全局管理器关掉顶层可见层（`core/esc-manager.ts:26-38`）。*

### 5. 搜完想清空，没有一键清除（效#6）
三个小别扭凑成一条：搜索框没有「✕」一键清词；从笔记点提醒定位进来时，搜索框被塞进完整长路径，想回全量列表只能全选手删或关面板；空态文案写着「或清除搜索」，界面上却没有这个按钮——承诺了但给不出。
- **A. 搜索框尾部加「✕」**（有词才出现，点了清词并把光标送回框）；空态有词时补一个「清除搜索」按钮。
- **B. 维持**。

★**建议 A**。

*备查：搜索壳仅图标+输入框（`render.ts:146`）；定位预填长路径 `ui.ts:482-486`；空态文案与按钮脱节 `ui.ts:622-624`。*

### 6. 搜到了但不告诉你命中的是哪个词（效#7）
搜索能滤出命中条目，但关键词本身不高亮，命中一多就得逐张卡用眼睛找。
- **A. 命中词加高亮标记**——一眼定位。
- **B. 维持**。

★**建议 A**。

*备查：命中判定止步于过滤（`ui.ts:236-239`），卡片渲染不接收关键词（`render.ts:209-222`）；备忘录卡是纯文本，加高亮成本低。*

### 7. 带链接的备忘录，按网址搜不到（效#8 半条）
卡片上明明显示了链接网址，搜索却只覆盖标题、场景、笔记路径等字段，网址不在其列——「记得那条里有个链接」时搜不出来。（本条原始报告的前半涉及已砍掉的清单功能，作废；只保留网址这半边。）
- **A. 搜索范围补上网址**。
- **B. 维持**。

★**建议 A**。

*备查：搜索五字段无 url（`ui.ts:237`），而卡片渲染了网址域名（`render.ts:175-179`）；补一行可解。*

---

## 三、条目整理（4 项）——改条目、挪场景、清旧账的事

### 8. 桌面上点卡片没反应，改一条要右键找菜单（效#10）
卡片悬停时有底色、看起来能点，点了却什么都没有；真正想改一条，得右键→扫菜单→点「编辑」。看完列表的下一个动作几乎总是「改一条」，这个最高频的下一步反而藏得最深。
- **A. 桌面双击卡片直接打开编辑**——单击保持留白不误触（标题带链接的点击语义不受影响），三步并一步。
- **B. 只把「编辑」提到右键菜单第一位**——改动更小，但仍要两步。

★**建议 A**。

*备查：卡面仅勾选/链接/位置三处接线（`ui.ts:665-692`）；hover 底色 `styles.css:91-93`。*

### 9. 把一条备忘录挪去别的场景，四步起步（效新-1）
备忘录靠场景组织，录错场景、事项换归属是常事；可「把这一条挪过去」没有直达通道——右键→编辑→在一排场景里点选→保存，手机上更长。讽刺的是「切换去看某个场景」反而是一键的。
- **A. 右键菜单/长按抽屉加「移动到场景」子项**——列出全部场景，点选即挪，两步完事。
- **B. 场景标签本身可点切换**——更快，但藏在卡片小字里，可发现性差、易误触。

★**建议 A**（日后可叠加 B）。

*备查：条目动作全集无场景项（`ui.ts:880-931`）；对照组场景筛选一键切换（`ui.ts:409-415`）。这是效率报告盘出的最大步数缺口（4+ 步）。*

### 10. 清理积压的已完成条目，一条三连击（效#11）
已完成折叠区天然是沉积层，想清掉十条旧账，就是十轮「右键→删除→确认」。没有批量出口。
- **A. 折叠条上加「清理更早已完成」入口**——弹窗列明将清条数，一次确认全清，支持撤销。
- **B. 维持逐条删**。

★**建议 A**（N 轮打断变 1 次确认；「更早」截断时按钮叫「清理已完成」）。

*备查：折叠区仅展开/收起/「更早 N 条」（`ui.ts:423-435`）；数据层仅单条删除（`data.ts:158-178`），需补批量删除口。*

### 11. 「更早 200 条」点开就收不回（效新-7）
已完成区的「更早 N 条」放全后，本会话内没有收回的路——误触或看完想收，只能关掉整个面板重开，上下文全部重置。
- **A. 放全后按钮变「收起」，可来回切换**；或收起折叠区时顺带复位。
- **B. 维持单向**。

★**建议 A**。

*备查：`ui.ts:430-435` 单向置位，唯一复位在下次开面板（`ui.ts:328`）。*

---

## 四、完成动作的反馈（2 项）——勾选之后界面给不给说法的事

### 12. 勾选后的三分之一秒，界面纹丝不动（效新-6）
勾选完成到划线之间有个 300ms 的小延迟（设计上是留给你反悔的机会），但窗口内勾选圈毫无变化——第一反应常是「没点上」再点一下，而第二击恰好是「反悔取消」：一来一回，条目被做了又解，动作凭空翻倍。
- **A. 窗口内给勾选圈一个「待定」样式**（半亮或呼吸），落定即划线、取消即摘除。
- **B. 维持无反馈**。

★**建议 A**（防抖可反悔是好设计，缺的只是窗口里的表达）。

*备查：`ui.ts:765-782` 排程后无视觉变化；勾选圈仅选中/未选中两态（`render.ts:204-206`）。*

### 13. 勾完的备忘录当场「消失」（MR2-4）
完成一条后，它立刻从未完成列表挪进「已完成」折叠区，而折叠区默认收起——看起来条目凭空没了，只剩计数 +1。想确认「确实勾上了」，反而看不到了。
- **A. 给个轻量去向反馈**——折叠条计数跳动或短暂高亮，知道它去哪了。
- **B. 完成后当次会话自动展开折叠区**——看得见，但多出来的展开态可能挡视线。
- **C. 维持静默归档**。

★**建议 A**。

*备查：完成区缺省收起（`memoShowArchivedByDefault` 缺省 false）；完成主路径全场景生效。与第 12 条同属「勾选之后给不给说法」，可同批做。*

---

## 五、面板与视图（3 项）——开面板、跳笔记、看空列表的事

### 14. 打开面板想直接记一条，得先点一下输入框（效#2）
备忘录面板打开后光标不在任何输入框里，最高频的「随手记一条」要多一次点击。编辑弹窗倒是会聚焦，主面板反而不会。手机端不聚焦是对的（一打开就弹键盘），桌面端可以跟进。
- **A. 桌面端打开面板即聚焦底部输入框**；从笔记定位进来时聚焦搜索框。
- **B. 维持**。

★**建议 A**（手机端维持不聚焦；Tab 圈住焦点的事由横切批接线负责，与本条不冲突）。

*备查：`openMemoPanel`（`ui.ts:315-489`）全程无焦点处理；定位预填分支 `ui.ts:483-486`。*

### 15. 跳转的笔记不存在，先把面板关了再报错（MR2-5）
点条目跳关联笔记或外链，第一步就是关面板，之后才发现笔记不存在、只弹一条通知——列表整个没了，想换一条跳还得重开面板。跳转成功先关面板合理，失败路径的代价偏大。
- **A. 先确认目标存在再关面板**；失败时面板留着、提示原因。
- **B. 维持先关**。

★**建议 A**。

*备查：`openItem`/`jumpToNote` 首步即 `closeMemoPanel()`（`ui.ts:726-748`）。*

### 16. 「今日」「重要」空了，还劝你「随手记一条」（MR2-7）
切到「今日」或「重要」没有条目时，空态文案是「这里还没有备忘录，随手记一条，别让它溜走」——其实别处可能有几百条，只是今天没到期、没标重要。搜索空态早就分场景说话了，这两个视图没跟上。
- **A. 两个视图各给专属文案**（如「今日没有到期或已完成的备忘录」「还没有标为重要的备忘录」）。
- **B. 维持通用文案**。

★**建议 A**。

*备查：`ui.ts:620-625` 空态未按伪场景分化。*

---

## 六、两端体验（3 项）——桌面和手机各自的小别扭

### 17. 桌面上没法用鼠标划选复制卡片文字（MR2-6）
条目卡整卡禁用了文字选择，想复制半句话，只能右键「复制内容」整条复制。手机上禁选是为了保住长按菜单，桌面上没必要陪绑。
- **A. 桌面端放开划选**（移动端维持禁选保长按）。
- **B. 维持两端都禁选**。

★**建议 A**。

*备查：`styles.css:91` `user-select:none` 含 webkit 前缀。*

### 18. 手机上编辑弹窗被软键盘盖住下半截（效#12）
手机上从「添加」进编辑弹窗会立刻弹软键盘，而弹窗高度不随键盘收缩——场景、优先级、截止、保存钮全压在键盘底下，得盲摸滚动。面板本体修过这个问题，弹窗漏了。
- **A. 弹窗高度改用随键盘收缩的单位、改为顶对齐**，保存钮行固定可见。
- **B. 维持**。

★**建议 A**（连带：移动端开弹窗不再自动弹键盘的半边属缺陷修复，本轮修复批一并对齐）。

*备查：弹窗壳 `max-height:82vh` 不吃键盘高度（`core/ui/components.css:597`）；面板侧已用 `--bz-vvh`+顶对齐（`styles.css:201-203`），弹窗无份。*

### 19. 手机上删除/延后只有长按一条路（效#13）
移动端整理条目全靠长按开抽屉再选动作；完成有勾选圈一步直达，删除和延后没有等价的快捷方式。列表滑动删除是常见习惯。
- **A. 先做「左滑删除」一档试手感**——与长按互斥、不妨碍滚动，好用再考虑延后档。
- **B. 不做，维持长按抽屉**。

★**建议 A**（先一档验证，不铺开）。

*备查：成本中等；若公共动作组件不带手势则域内自持触摸监听。*

---

## 七、确认框口径（1 项）——场景删除留不留确认的事

### 20. 删除一个场景，要不要也免确认（一致-旧2 半条）
全域删除口径已定「免确认+可撤销」，备忘录条目的删除正照此改（本轮修复批）。但「删除场景」不一样：它会把该场景下所有条目批量迁去别的场景、还要改设置，且目前没有撤销链——影响面大得多。审查建议场景删除单独保留确认框，留你拍板。
- **A. 场景删除保留确认框**，作为影响面大的合理例外（同密码本保留确认的先例）。
- **B. 场景删除也免确认**，但须先补上撤销能力再改。

★**建议 A**。

*备查：删除场景 `ui.ts:1494-1504`（确认框+批量迁移）；条目删除免确认=已定稿口径，归修复批（`ui.ts:838-870`）。*

---

## 来源对照表

| 呈报 | 原始编号（报告） |
|---|---|
| 1 | 效#1（memo2-efficiency 旧#1）＝ M3-11（memo2-ui）＝ 待接-1（memo2-consistency）＝ A4（memo2-arch），四报同源合一 |
| 2 | 效#4（efficiency 旧#4）＝ M2-1（memo2-ui）＝ 功能#1（memo2-func），三报同源合一 |
| 3 | 效#3（memo2-efficiency 旧#3） |
| 4 | 效#5（memo2-efficiency 旧#5） |
| 5 | 效#6（memo2-efficiency 旧#6） |
| 6 | 效#7（memo2-efficiency 旧#7） |
| 7 | 效#8 半条（memo2-efficiency 旧#8；清单半边随砍除功能作废） |
| 8 | 效#10（memo2-efficiency 旧#10） |
| 9 | 效新-1（memo2-efficiency 新-1） |
| 10 | 效#11（memo2-efficiency 旧#11） |
| 11 | 效新-7（memo2-efficiency 新-7） |
| 12 | 效新-6（memo2-efficiency 新-6） |
| 13 | MR2-4（memo2-ui） |
| 14 | 效#2（memo2-efficiency 旧#2） |
| 15 | MR2-5（memo2-ui） |
| 16 | MR2-7（memo2-ui） |
| 17 | MR2-6（memo2-ui） |
| 18 | 效#12（memo2-efficiency 旧#12） |
| 19 | 效#13（memo2-efficiency 旧#13） |
| 20 | 一致-旧2 半条（memo2-consistency 旧-2；条目删除半边为已定稿口径，归修复批） |

---

*拍板示例：「1A、9B、13C、19B，其余按★」——主线程将据此分派修复。*

---

## ✅ 拍板结果（2026-09-21 用户回复，权威执行口径）

> 用户原话：「1A 2A 3B 4A 5A 6A 7B 12A 13A 15A 16A 18A」「8 9 10 11 14 17 19 20不做」。

- **执行（12 项）**：1A 回车提交（bindFormSubmit 全口径，与在跑修复批同文件并单处理）；2A 保存防连点（=修复批 func#1 已在做）；4A 搜索 ESC 先清词；5A 搜索 ✕ 清词+空态清除按钮；6A 命中词高亮；7A 搜索补网址字段；12A 勾选 300ms 待定样式；13A 完成去向轻反馈；15A 跳转失败不关面板（与修复批 jumpToNote 定位修复同函数区并单处理）；16A 伪场景空态专属文案；18A 移动弹窗键盘适配（vvh+顶对齐）。
- **不做（8 项，零改动）**：8 桌面双击编辑、9 移动到场景子项、10 批量清理已完成、11 更早 N 条可收起、14 面板自动聚焦、17 桌面放开划选、19 左滑删除、20 场景删除确认框（不做的落点=维持现状，与★建议 A 保留确认框同效）。
- 分派：1A/2A/15A 并入在跑 memo 修复批（同文件同函数区）；其余 8 项待修复批合并后开「memo 拍板执行批」。
