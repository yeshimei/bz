# 备忘录 · 高级动效增强清单

> 2026-09-22 · 覆盖 `src/memo/` 全部交互面 · 共 13 条
> 基线 = worktree `wt/memo-strike` 最新状态（已含「卷走」与滑动底片）

## 怎么用这份清单

- **星级**：★★★ = 一望即知的质感跃升；★★ = 明显变顺；★ = 细节打磨
- **成本**：**S** ≈ 单文件几十行；**M** ≈ 跨 render/ui 两处 + 测试；**L** ≈ 需要新机制（FLIP / 行盒测量等）
- **已经帮你排除掉的**：
  - 完成 / 取消完成的「卷走」、侧栏场景与排序三档的滑动底片、排序三档平铺 —— 本批刚做，不重复提
  - 144 批次里 memo 的三条通病（删除可撤销 / 写盘失败提示 / 脏表单拦截）—— 功能项、已立项，不在本清单
  - 影院 396–409 那一系列动效批次是影院域的，memo 侧一件都没做

**一句话诊断**：`grep -c "transition" src/memo/styles.css` → **1**（还是折叠条箭头的旋转）。备忘录的动效基本是空白：除了刚做的两处，其余状态变化全是瞬变。而 core 的通知层有 19 个关键帧、影院有整套动效账本 —— 这个域是全域里最素的一个。

---

## 一、进出场（最缺的一层）

**1. 面板打开 / 关闭没有任何进退场** ★★★ 成本 S

现在：`openMemoPanel` 建完壳直接 `document.body.appendChild(overlay)`（`src/memo/ui.ts:384`、`386`），关闭时 `M.overlay.remove()`（`src/memo/ui.ts:610`）——两边都是硬切。core 的壳也没有动画：`.bz-panel-overlay`（`src/core/ui/components.css:811`）与 `.bz-panel-frame`（`:857`）都没声明 animation。
建议：遮罩淡入 140ms；面板体 `opacity 0→1` + `scale .97→1` + `translateY(8px)` 160ms `ease-out`，关闭先反向再 remove（复用 core 已有的 `bz-mask-in` / `bz-sheet-in` 语汇，`src/core/styles.css:797`、`:801`）。
为什么值得：21 个域共用这套壳，改 core 一处、全域面板一起升级——这是**单点收益最大的一条**。

**2. 删除条目时卡片凭空消失** ★★ 成本 S

现在：`deleteItemWithUndo`（`src/memo/ui.ts:1243`）删完 `await refresh()`，列表整块重写（`src/memo/ui.ts:862`），那条卡瞬间没了，只有 toast 说"已删除"。
建议：先给该卡折叠高度 + 淡出 180ms `ease-out`，播完再 refresh；撤销回插时同款反向淡入（新位置）。
为什么值得：删除是破坏性操作，"东西被拿走了"这个动作演出来比一句 toast 更让人放心，也让撤销入口那几秒不显得突兀。

**3. 编辑器弹窗没有入场** ★★ 成本 S

现在：`openEditor` 走 core 的 `uiModal`，遮罩直接铺满、弹窗直接出现（`.bz-overlay-popup`，`src/core/ui/components.css:652`）。
建议：popup `opacity + translateY(8px) + scale .98`，180ms `ease-out`；关闭先反向再移除。
为什么值得：备忘录的两个主要动作（新建 / 编辑）都要经过它，这是最高频的一屏。

**4. 新建条目落下时没有"落点"** ★★ 成本 S

现在：桌面 composer 提交（`src/memo/ui.ts:1357`）→ 落盘 → 列表重写，新卡直接出现在顶部，反馈只有 toast。
建议：新卡 `background` 用 `--bz-brand-soft` 高亮脉冲 700ms（复用折叠条的 `bz-memo-donebar-bump` 语汇，`src/memo/styles.css:130`）+ 自下 6px 淡入。
为什么值得：快速录入是备忘录最爽的路径（输入即存），但现在"存到哪了"要靠眼睛找。

## 二、列表重排与状态迁移

**5. 切场景 / 搜索过滤：整块重写，卡片无进出场** ★★★ 成本 M

现在：`content.innerHTML = sections.join('')`（`src/memo/ui.ts:862`）——内容换了就是换了；侧栏场景切换（`src/memo/ui.ts:469`）与搜索过滤（`:572`）都走这条路。影院已在 `issues/402-cinema-grid-motion.md` 做了 FLIP + 幽灵 + 接力，memo 一件没有。
建议：视图身份或关键词变化时，新卡 `opacity 0→1` + `translateY(4px)` 接力（前 12 张、步进 25ms、200ms 揭示曲线）；**同视图原地刷新不演**（否则每次勾选都闪一片，比不做更糟）。
为什么值得：切换视图是最高频的导航动作，现在是"啪"一下换一屏，没有任何连续感。

**6. 切换排序方式时列表直接跳** ★★ 成本 M

现在：刚做好的排序三档平铺（`src/memo/ui.ts:682` `applySortMode`）点一下 → `renderAll()` → 列表重排，卡片位置瞬间跳变。
建议：重排前记录各卡的矩形，渲染后对**留存卡**做 FLIP 补偿（`transform` 从旧位置位移到 0，220ms `ease-out`）。
为什么值得：你刚把排序提到工具行且做成了平铺钮——点击成本更低，跳变也就更频繁。这是那枚钮的"配套动作"。

**7. 已完成折叠区展开 / 收起是硬切** ★★ 成本 M

现在：点折叠条 → `M.showDone = !M.showDone`（`src/memo/ui.ts:480`）→ 重渲染；「更早 N 条」同理（`:487`）。只有箭头有过渡（`src/memo/styles.css:136`，全文件唯一一条 transition）。
建议：完成区包一层容器走 `grid-template-rows: 0fr ↔ 1fr`（200ms `ease-out`），展开时区内卡片再接力淡入。
为什么值得：折叠区现在只有"在 / 不在"两态，从"完成条数"到"看见是哪几条"这一步没有过渡可言。

**8. 完成的那一刻，折叠条只跳数字** ★★ 成本 S

现在：完成时卡片卷走飞进「已完成 N」，计数 +1、条子 bump ——但条目**名字**没有出现（`src/memo/ui.ts:1057` `rollTarget` 只取计数坐标）。
建议：卷轴落到计数的那一帧，折叠条右侧短暂浮出一行条目名（`已完成 · <标题截断>`），1.2s 后淡出。
为什么值得：让"我刚刚完成了什么"有一个名字。这是把现有动效从"好看"推到"有意味"的一条，成本很低。

## 三、局部反馈

**9. 计数数字是瞬变的** ★★ 成本 S

现在：主头行「N 项 · M 未完成」`countEl.innerHTML = mainCountHtml(...)`（`src/memo/ui.ts:715`），侧栏场景计数同理（`src/memo/render.ts:89`）——数字直接换。
建议：数字变化时旧值上移淡出、新值下移淡入（或 `scale` 弹一下），200ms；折叠条已有的 `bz-memo-donebar-cnt-bump`（`src/memo/styles.css:131`）可直接复用。
为什么值得：计数是列表状态最直接的读数，闪一下就能抓住余光，不必重新读整行。

**10. 搜索命中高亮整块跳出** ★ 成本 S

现在：`.bz-memo-hit`（`src/memo/render.ts:177` + `src/memo/styles.css:66`）命中词直接带底色出现，没有过渡。
建议：底色自左向右扫出（`background-size` 0→100%）200ms，或 `scaleX(.9)→1` 收一下。
为什么值得：搜到哪几个字是搜索的全部意义，值得一个"找到"的小动作。

**11. 移动端场景 chips 条没接滑动底片** ★★ 成本 S

现在：桌面左栏的场景选中块已由滑动底片接管（`[data-memo-nav]`，`src/memo/ui.ts:671`），但移动端的平铺 chips 条（`src/memo/ui.ts:760`）还是每片自绘底色、瞬变。
建议：把 chips 条也挂上同一套 core 机制（`src/core/ui/slide-pill.ts`），键用 `data-memo-scene`——触屏没有 hover，底片就只做"常驻 + 选中跟随"，跨片滑动依然成立。
为什么值得：同一套代码、同一处挂载点，桌面已经在了，移动端差一行；而且横滑 chips 时底片跟着走过去，观感提升比桌面更明显。

**12. 勾选圈的 300ms 反悔窗口只有"呼吸"** ★★ 成本 M

现在：点勾后有 300ms 可反悔窗口，期间勾选圈做呼吸动画（`@keyframes bz-memo-check-pending`，`src/memo/styles.css:1114`）。
建议：改成勾选圈边缘的**倒计时环**（SVG 描边 `stroke-dashoffset` 从满到 0，300ms 线性），呼吸作为环的底衬保留。
为什么值得：现在那个呼吸只是"有点事在发生"，用户并不知道还有多少时间可以点回去；倒计时环把"窗口还剩多久"直接画出来。这是本清单里最有"高级感"的一条。

**13. 卡片 hover 底色硬切** ★ 成本 S

现在：`.bz-memo-card:hover { background: var(--bz-surface-hover); }`（`src/memo/styles.css:144` 附近），卡片本体没有任何 transition。
建议：补 `background-color var(--bz-dur-fast) var(--bz-ease-out)`。
为什么值得：一个字的改动（加一行），但长列表里鼠标扫过去时不再一片闪。

## 附：确认过、不建议动的

| 项 | 为什么不建议 |
|---|---|
| 给标题做逐字包裹的划线动效（曾是「墨滴溶解」方案） | 全列表逐字包裹会让节点数翻几倍；已否，现用卡片级「卷走」+ 原生 `line-through` 兜底 |
| 弹窗/面板用大幅缩放或弹性回弹入场 | Obsidian 宿主原生观感克制，幅度大的入场会显得像另一个 App |
| 长列表逐条错峰（>12 张） | 低端机与长列表会掉帧；接力只做前 12 张就是为这个 |
| 给全域统一加动效 | 应该按域定"哪些环节该有、哪些故意不做"（影院有 `issues/400-cinema-motion-ledger.md` 这个范式），别全域一把梭 |

## 优先级速览

- **★★★（先做这两条，收益最大）**：1、5
- **★★**：2、3、4、6、7、8、9、11、12
- **★**：10、13
