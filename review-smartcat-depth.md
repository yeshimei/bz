# 小橘：记忆 / 情感 / 主动对话 深度建议清单

> 2026-09-19 · 覆盖 smartcat 域 47 个源文件（16810 行）· 共 18 条
> 触发：用户「小橘的记忆、情感和主动对话做的还是很幼稚，没有深度」

---

## 先给判断：不是机制少，是连线断

小橘有完整的三层：**感知**（各域观察）→ **情感**（PAD / 30 特质 / 信任依恋）→ **记忆**（GA 记忆流 + 洞察），
每层单独看都有像样的实现，理论底子对齐了 GA / MATE / Mehrabian 几篇论文，参数还跑过 RL 校准。

问题在出口。把所有状态的去向数一遍：

| 状态 | 算出来以后流向哪儿 |
|---|---|
| PAD 三维 | 5 档 emoji（`mood.ts:291`）、CSS 类（`mood.ts:283`）、prompt 里一行数字（`character.ts:214`）、字数系数 ±10%（`prompts.ts:57`） |
| 30 特质 | prompt 里一行数字（`character.ts:211`）、一句人设描述（`prompts.ts:67`）、字数乘子（`prompts.ts:35`） |
| 信任 / 依恋 | 界面上两个小数（`companion-context.ts:47`） |
| 缺席阶段 | dashboard 一张卡片（`dashboard.ts:475`） |
| 记忆检索 | prompt 里一段参考资料 |

**也就是说：状态只流向「显示」和「文本素材」，不流向「决策」。**
高兴不会让她主动多找你，低落不会让她安静一点，好奇不会让她多问一句，三天没见不会让她在对话里露出一丝情绪。

这是「幼稚」的根。参数调得再准，只要不接线，表现就不会变。

另外两条同源的根因：
- **她没有自己的内心生活。** PAD 的全部输入是：你的行为、你的情绪、你在不在场——没有任何一项是「她自己的事」。
- **她没有纵向的时间。** 每条互动独立结算，几小时衰减归零（半衰 10h / 7h / 3.5h，`mood.ts:210`），没有惯性、没有「连着几天」、没有「上次没说完的话」。

三条建议主线就照这个来：**接线 → 给她自己 → 给时间**。

---

## 怎么用这份清单

- **★★★** 杠杆最高（做完有明显体感变化）· **★★** 值得做
- **成本** S（≈一处改动）/ M（数据层 + UI + 测试）/ L（跨模块 + 数据结构）
- 编号就是让你挑的：回「采纳 3、7、12」即可。
- 已排除的（免得你觉得重复）：ADR-0021/0022/0023/0025/0036/0037/0039/0040/0041/0042/0043 已确立的机制本身；
  issues/123、141、160、163 已闭内容；`review-ux-suggestions.md` 里那条「设置靠长按打开无提示」见附注。

---

## 一、接通「状态 → 行为」

**1. PAD 算完只改了回复长度和猫脸，不影响任何决策** ★★★ 成本 L

现在：PAD 只流向 emoji / CSS 类 / prompt 数字 / 字数系数±10%（`prompts.ts:57`）。她的心情好坏，唯一可观测的效果就是回复长短和猫脸变一变。
建议：建一张 `mood → behavior` 纯函数表（可单测），接到主动引擎与话术选择器：pleasure 低 + arousal 高 → 提高追问比重；dominance 低 → 收敛话题长度与频率；arousal 高 → 气泡出现更快、更短。
为什么值得：这是「精妙参数」和「像个活人」之间唯一那道闸门。不接通，调参调到天荒地老也不会有变化。

**2. 30 个特质涨了没人消费，成长等于没发生** ★★ 成本 M

现在：特质只进 prompt 数字块（`character.ts:211`）、按 5 个阈值拼一句几乎不变的人设描述（`prompts.ts:67`）、当字数乘子（`prompts.ts:35`）。而 `DEEP_DELTA_SCALE=1/12`（`mood.ts:106`）让 δ 极小，几个月都跨不过阈值。
建议：挑 3–4 个高杠杆特质做行为开关——curiosity↑ → 多问；creativity↑ → 主动分享/发散；avoidance↑ → 少开口；每周生成一句「这周我好像变…了」的成长叙事（模板即可，挂在周报旁边）。
为什么值得：人格成长现在完全无形——用户看不到、感受不到，等于白算。

**3. 信任 / 依恋是两个看不懂的小数，没有「关系阶段」** ★★ 成本 M

现在：`信任 0.82 / 依恋 0.41` 直接印在陪伴上下文里（`companion-context.ts:47`）；trust 软收拢 0.85（`character.ts:160`），attachment 以 0.01 极慢跟随（`mood.ts:452`）。代码里「老朋友」只是硬编码文案（`index.ts:718`）。
建议：由 trust / attachment 派生关系阶段（初识 / 熟起来 / 老朋友 / 老交情），阶段决定话术与「权限」（只有老朋友阶段才允许使小性子、开玩笑）；阶段跃迁给一次有仪式感的表达。
为什么值得：关系是陪伴类产品的主线剧情，现在这条线是隐形的。

**4. 缺席状态机——最像人的设计，藏在一个没人点的卡片里** ★★★ 成本 S

现在：missing / reunion 只记一条自我事件 + 打 PAD 抖动（`absence.ts:330`），`absenceState` 只进 dashboard 卡片（`dashboard.ts:475`），**不进聊天 prompt**（companion-context 里根本没有它）。
建议：把缺席阶段 + 距上次在场天数写进聊天 / 欢迎回来 / 主动关心的 prompt，话术随阶段换——刚回来雀跃、第 3 天有点闷、第 7 天装作不在意。
为什么值得：投入产出比最高的一条。机制全部就绪，只差一根线。

---

## 二、给小橘一个「自己」

**5. 她所有情绪都来自你，你不在她就空转** ★★★ 成本 L

现在：PAD 的全部输入 = 用户行为（`mood.ts:299`）+ 用户情绪共振（`mood.ts:140`）+ 你在不在场。`registerEmotion` 只记标签、不写 PAD（`mood.ts:131`）。没有任何一个情绪源是「她自己的事」。
建议：建一个小橘自身状态源——她自己的每日精力（生物钟）、她自己在意的小事（从严选记忆里派生）、周期性自主扰动。哪怕只是每天一条跟她无关的心情底色。
为什么值得：一个角色的深度来自她有不依赖于你的部分。纯镜像注定幼稚——这是根因里最深的一条。

**6. 她永远温和：从不真的不高兴，也从不真的兴奋** ★★ 成本 M

现在：共振增益 positive:4 / negative:6（`mood.ts:61`），单次 angry 只 -2.1 愉悦（`mood.ts:67`），再经 10h 半衰（`mood.ts:211`）几小时归零，仍判 neutral。全代码没有「闹脾气 / 赌气 / 冷淡」的表达路径。
建议：允许性格性情绪（她自己的小情绪），在「被冷落 / 被误解 / 聊到她在意的事」时有更明确的反应；补 2–3 条带情绪的话术族（不满 / 别扭 / 赌气后自己找台阶）。
为什么值得：永远顺着你的角色像客服。有立场、会闹别扭才像朋友。

**7. 5 档猫脸把 PAD 的中间态全吃掉了** ★★ 成本 M

现在：5 原型 pleasure=[85,70,50,30,18]（`mood.ts:30`），取欧氏最近（`mood.ts:42`），pleasure∈[40,60] 基本全归 neutral。
建议：显示改「档 + 强度」两轴（"心情不错·七分"）；或者干脆把连续 PAD 直接喂话术选择器，不再先量化成 5 档。
为什么值得：中间态是「人味」最密集的地方，全被压成「平常心」最可惜。

---

## 三、补时间性：连续性

**8. 没有追问——聊过的像没聊过** ★★★ 成本 L

现在：全代码零命中「追问 / 跟进 / pendingTopic / unresolved」。主动关心每次一次性生成即销毁（`index.ts:656-722`），不留任何「待续」。
建议：主动关心或聊天结束时写入 `pendingTopic`（话题 + 待问 + 到期时间）；下次主动关心或欢迎回来时优先回捞——「昨天那事后来咋样了」。
为什么值得：这是「有深度」最直接的体感：连续性。人之所以是人，是因为记得没说完的话。

**9. 主动关心不读对话历史，只抓最近 3 条记忆描述** ★★ 成本 M

现在：聊天历史存 `conversationHistory`（默认 100 条，`config.ts:19,50`），但主动关心完全不读它——只取 `memoryStream.slice(-3)` 按 recency 拼（`index.ts:682`）。
建议：主动关心的素材检索优先命中近会话 + 高 importance 记忆，而不是「最近 3 条」。
为什么值得：她记得你「做过什么」，却不记得「我们聊到哪」，跨会话就是失忆重连。

**10. Bandit 的 reward 是 0/1，她分不清「敷衍」和「想聊」** ★★ 成本 M

现在：`responded ? 1 : 0`（`index.ts:611`），判据只是「10 分钟内你发过任意聊天消息」。一句「嗯」和一段长回复同分。
建议：reward 分级——回应长度 + 是否反问 + 情绪正向度 → 0.3 / 0.7 / 1。
为什么值得：现在的学习结果是「哪句话最容易让你回一个字」，于是反复强化最省事的那类废话。

**11. 自言自语那条定时器完全不过门控** ★★ 成本 S

现在：`generateAutoCompanionMessage` 走 5min×0.3 的 speakInterval（`interaction.ts:379-387`），不过 quiet-gate、不过作息、不分深夜。
建议：把同一条闸门（quiet / rhythm）接上；深夜降到极低概率且不提重话题。
为什么值得：这是「忽冷忽热」体感的来源之一——要么刷屏，要么几天不吭声。正好和 369 的事件触发一起做。

---

## 四、把记忆变成「回忆」

**12. 记忆条目只有「一句话 + 分数」，没有人的形状** ★★★ 成本 L

现在：`MemoryStreamEntry` = description + importance / emotion / credibility + 域事件元数据（`types.ts:194-219`）。没有时间情境、没有「和谁」、没有因果、没有悬念。
建议：观察落库时让 LLM 顺带抽 `context{scene, people[], cause, suspense?}`；检索回显时拼成「情境 + 事件」。
为什么值得：回忆的厚度来自情境。只有一句话的流水账，检索做得再好也不像「想起」。

**13. 记忆里不分这是「你的情绪」还是「我当时的情绪」** ★★ 成本 M

现在：emotion 是单标签（`types.ts:203`），且 chat 类多走词法落 `calm`（`memory.ts:636`）。
建议：拆 `userEmotion` / `agentEmotion` 两个字段；chat 观察用对话情绪分析补 userEmotion。
为什么值得：共情需要两个情绪并存——「你那天很烦，我在旁边不知道说什么好」，这种句子现在写不出来。

**14. 没有遗忘、没有模糊——她什么都记得斩钉截铁** ★★★ 成本 M

现在：记忆流注释明写「无上限」（`types.ts:229`），全程不裁剪；回显直接截 200 字（`memory.ts:1448-1468`），不暴露任何不确定措辞；credibility 只参与排序、不进表达。时间感 >7 天只剩日期（`memory.ts:1709-1722`）。
建议：① 表达层加衰减语感（久远 / 低可信 → 「好像是…」「我记得不太清了」）；② 时间感分档（近周 / 上月 / 本季 / 去年）；③ 冷存储席——极低分 + 久未访问降权，**不删**。
为什么值得：人会记不清，这恰恰是最有「人味」的特征；现在她像数据库，不像回忆。
（注：只动表达层与排序权重，与 085「记忆流无上限、不裁剪」的拍板不冲突。）

**15. 记错了没法纠正，错的东西会沉淀成「懂你」的结论** ★★★ 成本 M

现在：观察条目（记忆主体）没有任何纠错入口，只有洞察能 pin / 废弃（`insight-version.ts:56-63`）。日小结、周报直接进流成永久记忆（`memory.ts:685-697`、`memory.ts:1396`）。
建议：① dashboard 记忆条上加「这条不对 / 不重要」→ 降权 + 标记 → 反思证据池排除；② 日小结 / 周报先落「待确认」暂存，可驳回。
为什么值得：一条错观察会污染后续所有反思。而且用户能纠正，才说明这套记忆是可交互的，不是单向累积。

**16. 洞察只能「推翻」，不能「细化」** ★★ 成本 M

现在：洞察演化只有 `supersededBy` 一种语义（`insight-version.ts:92-117`）。
建议：加 `refines` / `qualifies` —— 后继挂在前驱下做「更精确的版本」，而不是删掉旧的；reflect prompt 补一句「若只是补充限定条件，不要 supersede」。
为什么值得：人的认知是慢慢加限定的（「他不爱社交——不过对你例外」），非黑即白的替换让结论显得武断、跳变。

---

## 五、松开表达端的刹车

**17. 主动关心只有 3 个抽象臂，话术是 200+ 条随机抽** ★★★ 成本 L

现在：臂 = `['empathy','life','vault']`（`index.ts:568`），选完臂内容来自 3 句 styleHint（`index.ts:686`）或每臂 2 条模板；PET 200 条 / CONNECTED 100 条 / WELCOME_BACK 85 条随机取（`messages.ts:350-469, 642`），且没有「上次说过什么」去重。
建议：落实 issues/369（域动静做成新臂池）；随机池接「近期记忆钩子」，欢迎回来改成「上次聊到 X」。
为什么值得：现在选不出「你具体干了什么」，只能说出「我注意到你最近常写东西」。臂是空的，Bandit 学得再准也没内容可讲。
（**注：351 已拍板（2026-09-17 用户「全按推荐」），实现票 369 状态仍是 open** —— 这条不是新建议，是提醒它还没落地。）

**18. prompt 用 7 条约束 + 4 次字数上限，把小橘压成安全问候** ★★ 成本 S

现在：`generatePrompt` 里约 7 条回复要求 + 重复 3–4 次字数硬上限（`prompts.ts:107-125`，absoluteMax 265 / base 180）；主动消息 user 指令只有「温和、简短、像老朋友」一句（`index.ts:718`）。
建议：去掉「简短」这类压制词，主动消息放宽到 ~260；system 里加 1–2 条「你们之间具体的梗」当 voice anchor。
为什么值得：人格描述只有一句合成词（如「温暖粘人」），再叠字数天花板，输出只能是无差别暖句。

---

## 如果只做三条

1. **第 4 条**（缺席进对话，S）—— 机制全就绪，一根线，立刻有人味。
2. **第 14 条**（记忆的模糊与时间感，M）—— 一次性改变「回忆」的质地。第 8 条（追问线，L）—— 体感最猛，但要动主动引擎，适合和 369 一起做。
3. **第 15 条**（纠错入口，M）—— 让记忆从单向累积变成可交互。

## 优先级速览

- **★★★（8 条）**：1、4、5、8、12、14、15、17
- **★★（10 条）**：2、3、6、7、9、10、11、13、16、18

## 附：确认过、不建议动的

| 项 | 原因 |
|---|---|
| 记忆流不裁剪、不去限流 | 085 追加拍板（走向量 top-N 召回）——第 14 条只动表达层与排序权重，不删数据 |
| 洞察 supersede 前置剔除（非乘法惩罚） | ADR-0039 已裁决，第 16 条是**新增**细化语义，不是改 supersede |
| 情绪温和共振（观察共振进 PAD） | ADR-0025 已拍板推翻旧口径，第 6 条是补「她自己的情绪」，不是回退 |
| 30 特质全量（不砍维度） | ADR-0023 对齐 MATE 全量，第 2 条是给它接消费端 |
| quiet-gate 三层结构（门控 / 每日上限 / 总开关） | ADR-0042 已定，第 11 条是补一个漏接的门 |
| 设置靠长按猫本体打开 | 已在 `review-ux-suggestions.md:264` 提过（补首次提示），不重复 |
