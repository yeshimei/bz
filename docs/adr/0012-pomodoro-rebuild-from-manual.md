# 0012 番茄钟按手册重建（原脚本代码已丢失）

番茄钟 QuickAdd 宏（`CONFIG/SCRIPTS/Quickadd/番茄钟.js`）代码已丢失——宏入口仍存在于 QuickAdd data.json，但脚本文件不存在，vault 仅剩使用手册（`CODE/obsidian 插件/番茄钟.md`，160 行，描述功能、无代码），脚本从未运行过故无任何遗留数据。项目铁律「数据格式零迁移」「逐字复刻」均以既有脚本/数据为前提，对番茄钟不适用。决定：番茄钟作为 bz 新域（ticket 26）**按手册重建**——不复刻代码（无代码可复刻）、无旧数据兼容义务、样式遵循 bz 统一 UI 风格（用户决策）而非手册描述的独立主题系统；数据采用新格式 `CONFIG/STORAGE/pomodoro.json` v1（跟随共享数据路径）。手册为唯一事实源，实现细节以 issue 26 为准。

**Consequences**: 未来若找回原脚本，仅需行为对齐（状态机/预设表），无数据迁移负担；spec.md 原「不在清单，本次不迁移」条目已同步移除。
