# 秋季批审查报告 · 体验线（2026-09-17）

范围同 bugs 报告。每条：动机 + 方案 + 成本。产品拍板类转问答线。

（审查进行中，逐节回填）

## memo（体验线）

1. `src/memo/render.ts:159` **P2** — composer `/词条` 语法零曝光。修法：placeholder 轮换或聚焦浮提示。
2. `src/memo/styles.css:339-342` **P2** — 移动端月历 chip 7px 热区 16px 低于手册 ≥44px（§8.2）、点色点直开编辑器主次颠倒。修法：::after 外扩热区，点格看清单为主、chip 长按编辑。
3. `styles.css:92-97` **P3** — chip 长标题无省略号、「还有 N 条」无可点感。
4. `render.ts:317-320` **P3** — 统计行与头行重复、翻月「今日」语义漂移。修法：并入 cal-head 或仅当月显示。
5. `render.ts:286-293` **P3** — 「回到今天」只翻月不选中。
6. `ui.ts:691-697` **P3** — 月历视图 composer 仍在，无 due 录入无去向反馈。
7. `ui.ts:977-996` **P3** — 完成反馈链三条过长。修法：合并单条。
8. `render.ts:170-216` **P3** — meta 标签 9 枚两行窄栏挤。修法：弱信息折叠。

已拍板待问（产品类）：无——上 8 条均为实现修补，待修复批统一处理。

## clipbook（体验线）

1. `report-ui.ts:174-178` **P2** — 本期空态与首次空态文案不区分，误伤数据安全感。修法：「本周还没读过 · 切到本月看看」。
2. `render.ts:376-382` **P3** — 空态改用 core uiEmpty 标准件 + 动作引导（手册 §8.3）。
3. `report-ui.ts:140-197` **P3** — 毫秒级聚合仍双 toast，小数据量应跳过。

## secondbrain（体验线）

1. `weekly-ui.ts:110-112` **P3** — 通知「·」连接违文案规范。改自然句。
2. `weekly-ui.ts:268-279` **P3** — 前 30 条截断无提示，补脚注。
3. `render.ts:383` **P3** — 撞车行点击目标与 CSS 语义矛盾，data-path 收敛到名字段。
4. `weekly.ts:272` **P3** — 入口卡旧区间加「最近一份」前缀或 14 天降隐。
5. `CONTEXT.md:222` **P3** — 词条补 issue 360 记载。
6. `mobile-panel.ts` **P3** — 移动参考抽屉补周报入口（或拍板接受命令单入口）。

## favorites/review（体验线）

- favorites：管理入口不可达（面板内加「管理」钮）；seed 个人化+无恢复默认；零计数标签无显示。
- review：档位 chips 人话化；未选文件夹 meta 矛盾文案；小结补「错题留在题库」；放弃弹 0% 正确率观感差。

## belongings/diary/pomodoro（体验线）

- belongings：① **P2** 纯离场年（有出离无购入）分类段「共 0 类」+12 根零柱像渲染坏了，缺人话空态；② 当年日均走势当月列恒空档，改 as-of 今天注「截至今日」（P3）；③ KPI 卡纯静态不接续面板筛选肌肉记忆（P3 增强）；④ 翻年 toast 连闪，改 quiet finish（P3）。
- diary：① **P2** 年份角标压在摘要字上 + 3 行 caption 被卡裁半，长摘要卡面破（修法：padding-bottom/角标移位/降 2 行，真机验证）；② 文字卡点击=关面板跳走与媒体卡灯箱落差，易误触（P3）；③ title 只「日期 时刻」，应拼摘要全文（P3 零成本）。
- pomodoro：① 首次「近 6 月」6 根 2px 零柱像坏了，加「自本版起积累」人话占位（P3）；② 月柱按 count 归一看出不小时对比，改 minutes 归一+柱顶小时数（P3）；③ tab 热区 24px 低于 44px 规范（P3）。
