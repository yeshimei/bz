# 05 — 实时同步：笔记编辑 → 索引与面板跟随

**What to build:** 笔记即事实源（ADR-0015）的运行时承诺：用户在 Obsidian 里**改黑匣子笔记的标题或内容**，黑匣子主面板/AI 上下文实时跟随。内容编辑走 metadataCache 变更事件（命中 `黑匣子/` 下文件 → 索引刷新 + 面板打开时实时刷新，保留筛选与滚动）；改名/删除/新建走 vault 事件（索引按 frontmatter id 重映射/移除；改名后全库 wikilink 由 Obsidian 原生自动更新）。索引指向缺失文件时面板/检索静默跳过该条。

**Blocked by:** 01 — 数据层笔记化

**Status:** done（实现完成，1092 测试全绿）

- [ ] 内容编辑（metadataCache changed）命中黑匣子笔记 → 索引刷新 + 面板打开时实时刷新（保留类型筛选、搜索词与滚动位置）
- [ ] 改名/删除/新建（vault rename/delete/create）→ 索引按 id 重映射/移除/新增；删除后面板不残留该条
- [ ] 索引指向缺失文件 → 面板/检索跳过该条不报错
- [ ] 对话/复盘/事件提炼读取时总是读最新笔记内容（不缓存正文）
- [ ] 测试：mock vault 事件（modify/rename/delete）→ 索引重建 + 面板刷新断言
