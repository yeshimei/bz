# 04 — 主面板微调：标题「黑匣子」+ 搜索框切换

**What to build:** 黑匣子主面板两处 UI 调整：① 标题去掉 emoji 与「N 条内容」计数，只显示**「黑匣子」**（DOM id 保留，外部约定不动）；② 搜索框**默认隐藏**，header 右上角新增 **🔍 图标（位于 ⚙️设置 前）**切换显隐，显示时图标高亮；**隐藏即清空已输入关键词并重渲染**；每次打开面板回到默认隐藏。搜索框显示时宽度 100%，防抖/范围语义不变。

**Blocked by:** 01 — 数据层笔记化

**Status:** done（实现完成，1086 测试全绿）

- [ ] 面板标题显示「黑匣子」（无 🕳️、无「N 条内容」）；`bz-blackbox-panel-title` DOM id 保留
- [ ] 搜索框默认隐藏；🔍 图标在 ⚙️设置 前，点击切换显隐，显示时高亮（.on 态）
- [ ] 隐藏搜索框即清空已输入关键词并立即重渲染；每次打开面板回到默认隐藏
- [ ] 测试：标题文案/切换显隐/清空重渲染（jsdom 交互）
