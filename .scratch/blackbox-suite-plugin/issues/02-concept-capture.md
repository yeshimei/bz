# 02 — 概念录入改造：双输入框 + 按钮内容判定 + 锁定 + 概念直达命令

**What to build:** `bz-blackbox-capture-concept` 直达命令（跳过类型选择，保存后直接关闭）。概念录入页改为**概念名输入框（单行）+ 文本输入框（textarea 自适应行高 ≤8 行，复用 memo 域 auto-grow 先例）**；主按钮按文本输入框**内容**判定：「生成卡片」（空）/「确定录入」（有内容，无论 AI 生成还是手动输入），无重新生成入口。选中文字自动填充概念名并**锁定只读**（无选区时手动输入可编辑）。保存写概念笔记（名/定义/关联区双链）+ 索引更新。core 层新增**选区读取助手**（选中文字 + 起止位置快照，供本 ticket 自动填充与 ticket 06 注入复用）。

**Blocked by:** 01 — 数据层笔记化

**Status:** done（实现完成，1077 测试全绿）

- [ ] `bz-blackbox-capture-concept` 命令注册（main.ts COMMANDS + smoke 清单），打开直达概念录入，保存后弹窗直接关闭
- [ ] 概念页：概念名输入框（单行）+ 文本输入框（自适应 ≤8 行）；按钮文案按文本框内容判定（空→「生成卡片」/ 非空→「确定录入」），无重新生成入口
- [ ] 生成卡片：AI 写定义进文本输入框（可编辑）；AI 不可用降级直接录入（定义=概念名可编辑，永不拒收）
- [ ] 有选中文字时概念名自动填充且只读（锁定）；无选区时手动输入
- [ ] 保存：概念笔记（frontmatter + 定义正文 + 关联区 `[[…]]`）+ 索引更新；旧 `bz-blackbox-capture` 引导式的连接展示逻辑保留
- [ ] 测试：按钮条件/锁定只读/生成与保存落盘/保存即关（jsdom + mock AI）
