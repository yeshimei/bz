# 03 — 标签配置、类型与状态

**What to build:** 日记本领域基础：默认主/二级标签配置（emoji 编码）、文本格式与 JSON 格式的标签配置解析、标签↔emoji 双向映射构建、标签辅助函数（主/二级判断、父标签、可用标签列表）、日记条目类型、全局状态对象（UI 态/数据态/事件态分组）。解析失败回退默认配置。

**Blocked by:** 01 — 工程骨架与空插件装载

**Status:** ready-for-agent

- [ ] 文本格式解析：`标签 emoji` 行、`主标签 emoji > 子标签 emoji, ...` 行、非法行跳过、`>` 多重内容
- [ ] JSON 格式解析与失败回退
- [ ] 双向映射：emoji→标签（含组合 emoji 如 🧑🎨）、标签→emoji，未知回退 📖
- [ ] 标签辅助函数与排序列表（有二级标签的主标签隐藏）
