# 43 — 黑匣子时间线标签页 + 画像事件投影

**What to build:** 🕐 时间线标签页（五标签面板框架已在 ticket 41 落地）端到端：按年月分组的事件流（事件卡：日期+标题+参与人物+情绪色点+证据链入口）；推测事件虚线 + ❓ + [确认]/[删除]；筛选（人物/年份）；人物标签页详情内嵌事件投影（按人过滤）。

**Blocked by:** 42 — 黑匣子事件提炼全栈

**Status:** ready-for-agent

## 验收标准

- [ ] 时间线标签页：按年月分组纵向事件流；事件卡 = 日期 + 标题 + 参与人物（主角完整/配角折叠）+ 情绪色点 + 证据链入口（点击展开来源条目摘要）
- [ ] 推测事件卡：虚线框 + ❓ 标记；[✓ 确认]（转实线）/ [✕ 删除]（数据删除；不做忽略持久化清单，遗忘权后置）；删除后不残留
- [ ] 筛选：按人物（画像）/ 按年份
- [ ] 画像详情内嵌事件投影：该画像参与的事件按时间排列（与全局时间线同源，数据单份）
- [ ] 设置「推测事件显示」开关为关时时间线隐藏推测事件（开关本身在 ticket 45 设置页落地，本票消费该设置字段）
- [ ] 测试：年月分组纯函数、推测卡确认/删除、人物/年份筛选、画像投影一致性

## 引用

- `.scratch/blackbox-suite-plugin/spec.md`「主面板」「样式」节
