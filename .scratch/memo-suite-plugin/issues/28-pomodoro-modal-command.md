# 28 — 番茄钟弹窗与主命令

**What to build:** 用户可用的第一片：命令 `bz-pomodoro-open`（中文名「番茄钟」，icon timer）打开中央单例弹窗（复用 escManager，重复打开幂等聚焦）；弹窗含环形进度 SVG、阶段文案（专注/短休/长休 + 第几个循环）、剩余 mm:ss、开始/暂停/重置/跳过按钮（强制专注模式下暂停/跳过/重置禁用）；1s tick 驱动 UI 刷新；关闭弹窗计时在内存后台继续。命令注册进主命令表（域内不重复 addCommand），smoke 命令清单同步 +1。

**Blocked by:** 27 — 数据层与状态机。

**Status:** ready-for-agent

- [ ] `bz-pomodoro-open` 注册生效，命令名「番茄钟」，icon timer；smoke 命令清单 +1
- [ ] jsdom 测试：打开弹窗渲染（环形进度/阶段文案/mm:ss/按钮/⚙️ 入口）、开始→倒计时走、暂停/恢复、重置回满并停止、跳过流转、强制专注模式下禁用暂停/跳过/重置
- [ ] 单例语义：重复打开不重复建 DOM（幂等聚焦）
- [ ] 关闭弹窗后计时继续（重开显示剩余时间正确）
- [ ] 弹窗 UI 样式遵循 bz 统一风格（非手册独立主题，ADR-0012 用户决策）
