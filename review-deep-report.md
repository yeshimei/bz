# 包仔插件 · 全域深审修复循环 · 收官总结报告

> 2026-09-20 · 覆盖范围：21 个功能域中的 **18 个全流程深审修复**，另 recap（回顾）按拍板只标注、备忘录排队尾、小橘 / 第二大脑 / 知识盒三域按您此前拍板不在本轮范围
> 状态：**循环 + 拍板执行 + memo 重审全部收官**；64 项拍板与 memo 20 项拍板均已执行或拍板不修，最终部署 53b2c231

---

## 一句话总结

这轮「全域深审修复循环」把插件的 18 个功能域挨个做了一次「地毯式体检」：每个域 5 个审查视角并行扫描 → 发现问题去重入账 → 聚簇派批修复 → 每批自带回归测试 → 串行合并、全量测试通过后才构建部署。**约 300+ 项发现全数入账并修复上线**（备忘录除外，原因见「遗留与挂账」），回归测试从 6232 例增长到 **6818 例，全绿**。64 项「要不要做、怎么做更好」的体验类选择您已批复： Wave1 五批（39 项）已合并上线，Wave2 两批（触屏悬浮隔离范式、大面板键盘圈闭、归物本键盘与空态、密钥型输入档位等 4 类横切项）执行中。

### 数字总账

| 项目 | 数字 |
|---|---|
| 深审范围 | 18 域全流程闭环 + recap 标注完结 + 备忘录队尾 + 3 域排除 |
| 发现 | 约 300+ 项全数入账（P1 级 9 项、P2 级 40+ 项、P3 级 200+ 项，另有一批体验建议） |
| 修复 | 20+ 个修复批全部合并并部署，每批带回归测试，多项「修复前必红」的守卫用例 |
| 测试 | 6232 → 6818 例，全绿；每批合并前全量门禁（测试 + 类型检查）通过 |
| 体验拍板 | 64 项已批复：39 项执行上线（Wave1 五批）、23 项不采纳不动、其余随 Wave2 收尾 |

---

## 这轮是怎么干活的

每个域走同一套流程，全程留痕可复核：

1. **五路并行深查**：功能正确性、UI 缺陷、操作效率、风格一致性、架构与测试覆盖，各派一个只读审查代理同时扫，另复核历史旧账是否还在；
2. **去重入账**：五个方向的发现合并去重、按风险定级（P0 丢数据 / P1 功能错 / P2 硬伤 / P3 瑕疵），每个域一段台账落在 `review-deep-bugs.md`；
3. **聚簇修复**：按文件分工拆成若干修复批（互相不碰同一片代码），在独立工作树里并行开发，每批必须自带回归测试，多项还配了「修复前必红」的守卫用例；
4. **串行合并发布**：逐批合并进主分支、解冲突、全量测试与类型检查全绿后构建部署，再清掉工作树。

18 个域跑完，就是 18 轮这样的循环；后台无人值守拿不准的事，一律选低风险侧执行并记录理由，真正需要您取舍的都攒进了拍板文档。

---

## 这轮最该知道的三类收获

### 一、五类「各抄各的」代码收成了单源

同类工具代码过去每个域各抄一份，抄着抄着就走样了。这轮收编成一份：改一处全域生效，还有测试守着不许再抄第二份。

- **YAML 转义**：三个域三种写法，其中一种遇到反斜杠就把数据写坏——统一成共享层一套转义工具，三域同改，防止冒出第四份；
- **日期解析**：「今天」怎么算，先后出现过六份域内复写——归物本成为最后一个退役的，全库只剩一份；
- **HTML 转义**：游戏库自写的一套少了单引号保护，正是它出毛病的病根——全数退役收编共享层；
- **触控热区**：手机上按钮命中区不足 40px 的问题，修法是给基础组件加一个修饰类，一处生效全站，各域挂上类名就复活；
- **失败通知**：写盘失败过去有的域静默、有的域说黑话——统一走一个人话通知单源，处处自带「重试」按钮。

### 二、全仓唯一的模块环解开了

首页和回顾两个模块互相引用成了环（A 找 B、B 又找 A，加载顺序一变就出事），这是全仓唯一一处域间环。已把共用函数下沉到共享层解开，并加了静态守卫：今后谁再绕环，测试直接红。

### 三、三处「老复发」的机制上了契约锁

有些毛病反复复发，是因为没有任何东西拦着它再犯。这轮给三处最容易复发的位置上了自动化测试的锁：

- **体检白名单**：哪个域给数据文件加了新键、忘了同步体检白名单，体检就永远误报「约定外数据」——现在「实际写入的形状」与「白名单」必须恒等，键一漂移当场红；
- **收藏夹域事件**：事件的发生方与消费方各认各的名单，改了名谁都不知道——现在两侧对账恒等，契约锁死；
- **首页行为流**：记录文件路径各写各的——收成单源并加对账测试。

---

## 逐域速览（18 域 + recap）

每域讲三件事：发现了什么要害、修了什么、有什么要您拍板的。

### 1. core 共享层——所有功能共用的地基

- 要害：禁用再启用插件后全站 ESC 永久失效；设置弹窗在防抖窗口内关闭会静默丢最后一次编辑；构建脚本把日记样式登记了两遍，打包产物里有 1734 行重复样式。
- 修复：ESC 管理器改「软关」可重挂；关面板前先把改动落盘；触摸热区修饰类一处复活全站；一批键盘可达性、焦点与监听清理问题；五批修复，新增回归约 104 例。
- 拍板项：无。

### 2. diary 日记本

- 要害：给加密日记改标签时，若保险箱恰好自动上锁，保存静默失败还装成功；写日记弹窗按 ESC 整面消失、稿子全丢；信件时间取自文件创建时间，复制文件就变。
- 修复：保存整链补错误提示；弹窗接 ESC 分层、「要放弃吗」确认、补取消钮；「1 月 31 日改到 2 月变成 3 月 3 日」的日期漂移根治；墙会响应外部新建文件；大附件加密加 64MB 守卫。
- 拍板项：无（内联样式等旧债登记渐进消化）。

### 3. clipbook 剪藏本

- 要害：重剪覆盖用的是全仓最后一个手搓确认弹窗，且不告诉你「覆盖会冲掉 AI 摘要和手工编辑」；面板层级全站唯一漏发号，被别的面板压住「命令像失灵」；同一个词锚定第二个笔记会产出嵌套破链。
- 修复：覆盖确认迁统一弹窗并新增第三个出口「另存为新剪藏」；层级收口；写盘假成功改为立即报错；批量已读补撤销；j/k 阅读快捷键焦点接力；目录里无法识别的剪藏不再无声消失，改给计数提示。
- 拍板项：4 条历史搁置项重议（移动滑动手势、正文参与搜索、UP 管理 Cookie 行、覆盖时字段级合并）。

### 4. review 复习

- 要害：批量出题「先清空旧题再去生成」，AI 一失败旧题不可逆丢失；记忆算法难度值域失真，评「简单」难度反而上升；复习进行中按回车会穿透确认框直接交卷。
- 修复：清空挪到生成成功之后；难度值域统一；复习循环与冲刺加防重入守卫；难度弹窗迁统一弹窗；删除/移出对齐「免确认+可撤销」；日期、转义等收编共享单源。
- 拍板项：14 项分流待拍板（真阈值排期、卡片正文摘要预览、命令改名等，都在拍板文档）。

### 5. encrypt 保险库

- 要害：移动端整套样式在早前一次清理中被连坐误删，手机上保险库界面是坏的；「加密日记」入口被一次无人值守的提交悄悄撤掉，与既定拍板相反；「还原回日记」不复算当前目录，条目还原后凭空消失；还原失败的「重试」会永久卡死。
- 修复：从历史版本取回样式并加冒烟断言防再删；按权威拍板恢复日记资产入口；还原逻辑下沉单源并复算目录、竞态入队；大附件 64MB 守卫；上锁时顺手收场预览与弹窗；四批合并。
- 拍板项：8 项分流（笔记批量操作、首建流程统一、四条旧建议重议等）。

### 6. password-vault 密码本

- 要害：添加/编辑弹窗被一张默认尺寸的内嵌图标撑坏，必现横向溢出；别的域上锁后这里只剩一块没有输入框的空锁屏、无法解锁；「安全模式」判定三处分叉，用旧设置键的用户永远不会自动上锁。
- 修复：补齐样式；外部上锁补锁屏；安全模式实时双键读取+双向同步；补齐 15 分钟闲置自动上锁；删除挂上撤销通知；快速取密回车改「复制命中项」不再误生成。
- 拍板项：9 项分流（删除确认框去留、生成密码落库、批量导入导出、移动端两处不便、首建流程统一等）。

### 7. cinema 影院

- 要害：建档模板里多行影评裸插进 YAML，影片会从面板消失、豆瓣自愈链跟着断；把「已看」改回「想看」时影评被静默清空；搜索一落空态焦点就丢，接着打字全部无效。
- 修复：模板改走统一 YAML 通道并配往返测试；影评保留不误删；测试环境的 frontmatter 解析假层改「遇错即报」（此前真机才会坏的缺陷测试根本测不出来）；移动端补空态；卡片键盘可达、滚位记忆、搜索 ESC 清词。
- 拍板项：6 项分流（季圆点热区、抓取失败加重试钮、分析页图标等）。

### 8. belongings 归物本

- 要害：面板关闭收口不全，孤儿表单能把数据写到已关闭的会话外；价格没有上限校验，手滑输个超大数读回来变 0；删除是全域最后一个还带「确认+撤销」双保险的活跃域。
- 修复：两批 28 项——关闭单路径收口；价格钳制加人话提示；日期口径单源；删除改免确认直达撤销；网格卡键盘可达；搜索 ESC 二段+✕；报告「回收」口径与统计层统一。
- 拍板项：7 项分流（移动端资产筛选入口、物品图标摘不掉、0 元日均显示等）。

### 9. bookshelf 书库

- 要害：「继续在读」绕过统一的视图入口，报告页里触发会让整个面板假死只能 ESC 逃生；冷开报告后「返回书库」永远挂着加载占位；读书笔记功能约 900 行代码但界面再也进不去，四处文档却宣称可用。
- 修复：单批 24 项——视图统一走一个入口；批注引号往返反转义；数据文件损坏给一次性告警；依赖方向理顺；检索 ESC 二段+✕、滚位保持、热区补齐。
- 拍板项：读书笔记去留（全清单最大的一项：恢复最小入口 or 整体退役并改文档）等，均已并入拍板文档。

### 10. gameshelf 游戏库

- 要害：自动同步链上有断点——首次打开或新游戏入账后，媒体本地化、中文名回填三条后台队列根本不启动，「拉到的都存本地」的承诺在主路径不兑现；后台节流重渲会整刷面板，打字失焦、输入法被打断。
- 修复：断链接通；重渲改「轻刷」并保焦点快照；域内自写转义收编共享层；回填与重渲染的竞态治理；媒体下载加 20 秒超时；两批合并。
- 拍板项：4 项（已下架手改被同步翻回、Steam 密钥明文框、面板滚位记忆、最近玩过行键盘可达）。

### 11. favorites 收藏夹

- 要害：删除/归档仍走「确认框+撤销」双保险，是全域最后一个滞后域；编辑保存全量覆盖写回，会悄悄回滚掉体检修复过的数据；标签撞名哨兵值会让筛选不可达。
- 修复：删除/归档改免确认直达撤销（相关文档误记三处一并勘误）；读侧加归一管道；编辑改窄化合并写；域事件加契约锁；撤销补发领域事件；词条误植段迁正。
- 拍板项：6 项（无链卡点击零反馈、点卡直开外链无标识、磁贴行限高、触屏 hover 粘滞全域议题等）。

### 12. pomodoro 番茄钟

- 要害：「月档统计按分钟说话」的修复只写了函数没接线——三处注释宣称的行为从未发生过；番茄钟的归档数据没进体检白名单，正常数据每次必被误报。
- 修复：单批 20 项——月档接线完成（以消费点逐一核查为完成标准）；体检白名单补齐并上契约测试；专注中重置加确认框（此前拍板的执行）；卸载竞态一处护栏三症状同愈。
- 拍板项：4 项（读屏播报、「停止专注」一词三义、统计档位跨重启记忆、空月份占位文案）。

### 13. home 首页

- 要害：全仓唯一的模块环就长在首页与回顾之间（裁剪周报死代码前必须先解环）；首页采集一次要重复读盘解析影院数据 7 遍；「全部域」入口列在矮窗口必被裁断且不可滚。
- 修复：解环（共用函数下沉共享层+静态守卫）；采集窗口裁剪、一次采集多处复用；入口列滚动兜底；移动端高度、热区、滚位、焦点全套补齐；home.json 白名单升级并契约双钉。
- 拍板项：H 系（周报死代码裁不裁、外观「布局/皮肤」占位键、骨架期标题闪现等）。

### 14. checkup 数据体检

- 要害：白名单漂移让体检「狼来了」——正常库恒报约定外数据段；体检自带的修复功能里有一处本地实现与剪藏本语义分化，非字符串网址会被误报残留、一键修复会清掉真数据。
- 修复：4 处白名单对齐 + 契约测试根治（15 例恒等锁）；「自产自报」闭环（零命中的修复不再凭空落盘）；目录单源四处收编；大库体检进度插值、撤销通知聚合。
- 拍板项：CK 系（可选段豁免、按域分组修复、单条修复确认框、进度条 75% 即切页等）。

### 15. reading-report 阅读报告

- 要害：两条最严重项同根——桌面报告视图没有任何返回出口，进来出不去；宿主数据接口两套镜像且无契约守护，EPUB 分类的四项怪象全是这一个结构性根因；速度段还有「编造值上屏」。
- 修复：返回钮恢复并加防回潮守卫；供数面收敛为单源+契约对照测试（四项怪象随刀消）；主计算热点从平方级改单趟归并；编造数据的统计段删除、死字段一刀清。
- 拍板项：RR 系几项；残款 RR-U13（书库宿主搜索框）移交书库收尾批。

### 16. settings-panel 设置面板

- 要害：面板渲染器是共享层的「行为复刻」版——共享层历轮加固（落盘兜底、数值钳制、容错）全都不传导到面板，五项缺陷同一根因；自绘下拉按 ESC 直接关掉整个面板，纯键盘改不了任何下拉型设置。
- 修复：内核下沉共享层导出、一次收口（五项随刀消）；下拉全重写对齐共享范式；搜索补防抖、命中徽标重算、大小写不敏感；「重置本域」确认框回车改落安全侧；左栏 roving tabindex。
- 拍板项：SP 系（搜索键盘闭环、命中词高亮、缓存与滚位记忆等）。

### 17. auto-summary 自动摘要（收官域）

- 要害：YAML 转义三份三形态，本域那份遇到反斜杠就把值写坏（最久的一条旧账）；「整文件重写」会把用户自加的属性类型悄悄改掉；AI 返回无校验，曾真实发生过把笔记改名为 `[object Object]`；队列没有失败熔断，AI 故障时每篇完整重发、火上浇油。
- 修复：转义工具落共享层单源、一次收编三域；改「只重写管辖的三个键、其余原文保留」；AI 结果加 schema 校验；失败退避熔断+聚合成一条通知。
- 拍板项：批量补缺口入口、队列取消入口两项。

### 18. attach 附件

- 要害：搬附件找引用用的是纯正则——代码块里的引用会被误搬断链、大小写不同的链接漏搬（保险库早有更聪明的找法，本域没用上）；一次搬移把同一份清单重复算了三遍。
- 修复：找引用改「缓存优先、正则兜底」对齐保险库，误搬+漏搬一并根治；扫描一次收集三段透传；失败给明细+重试；全选/反选、撤销进度、中止出口等批量体验补齐；「资源」改口「附件」。
- 拍板项：AT 系（动词统一、跳过预览快捷通道等）。

### recap 回顾（只标注，不投资）

按您的拍板，退役面板不再花审查预算；它的纯函数库仍被首页时间线消费，质量已由首页域轮交叉覆盖。将来若复活面板，再按「新域」流程重新深审。

---

## 遗留与挂账

- **备忘录（队尾，需要您确认两件事）**：此前五批修复（周期锚定日被剥、iPhone 坏日期、双击双存等 40+ 项）曾完成上线，但您随后把备忘录整体回滚到引入周期/月历/子任务之前的版本，这批修复随滚不在当前代码。已排入队尾，将按回滚后的代码重新深审。重审前请您确认：① 这次回滚的原因（记档在案，避免重审时把「故意砍掉的功能」误判成缺陷）；② memo.json 旧数据的兼容策略。
- **RR-U13**：阅读报告里「书库宿主搜索框可点但只刷隐藏墙」的问题，已排入 Wave2 收尾批。
- **A6 跨域键字面**：~~归收尾统一批改引常量单源~~ 已随 Wave1 体检附件批收口（根 settings.ts 五键 + 剪藏本五处改引 keys.ts）。
- **登记不修（在案，非遗漏）**：一批有缓解措施的性能项（如日记写一笔全库重读）与技术债，等真实卡顿或触碰时渐进消化。
- **体验拍板不采纳项（23 项）**：按您的批复不做任何改动，清单在 `review-deep-ui-pending.md` 拍板结果节（含 46「图标不触发改动检测」您明确不修，后续不再重提）。

---

## 拍板结果与执行进展（2026-09-19 批复）

您的批复：**「5B 6B 12A，其他按照你的来」+ 23 项不采纳**。已按此执行：

- **已上线（Wave1 五批，39 项）**：复习域十项（做题拦截提示、读盘降频省电、题面去 emoji、统计弹窗焕新、命令改名、冲刺模式行、键盘三处、空态统一）；影院四项（表单聚焦、季圆点热区、豆瓣重试、分析页图标）；游戏库两项（最近玩过行键盘化、滚位记忆）；番茄钟两项（停止专注拆词、统计档位记忆）；密码本五项（生成前确认、空值复制提示、移动端切换与网址可点、快速取密键盘）；保险库两项（图标收编统一体系、搜索框收编）；体检八项（单条免确认、可选段豁免、按域分组修复等）；附件三项（动词统一、跳过预览、已在目标前置预告）；收藏夹三项（点按反馈、外链角标、行限高）；首页两项（骨架不闪、裁周报死代码）；设置面板三项（搜索键盘闭环、命中高亮、滚位与缓存记忆）。
- **执行中（Wave2 两批）**：触屏悬浮隔离范式、大面板「打开即入焦 + Tab 圈闭」全域推广、保险库/剪藏本工作台键盘化；随后归物本键盘与空态收尾、密钥型输入档位（Steam 密钥防瞄屏）、RR-U13。
- **不采纳（23 项，零改动）**：首建流程统一、右键入口显化、读屏播报、集合外标签示型、过滤空态出路、零柱占位、读书笔记孤岛、移动端资产筛选、R 目标阈值、队列懒加载、卡片正文摘要、保险库批量操作、生成落库、导入导出、四条旧建议、图标清除、图标改动检测、手改已下架翻回、占位键标注、滑动手势、剪藏正文搜索、UP Cookie 行、字段级合并。

---

## 下一步

- **全部收官**：64 项拍板（39 项 Wave1 + 12 项 Wave2 落地、其余拍板不动）与 memo 重审（字段清除 + 90 条发现的功能缺陷全修 + 20 项体验拍板执行）已全部合并部署，最终门禁 470 文件 / 6977 例全绿。个别条目实现与预期不符的话，指编号即可微调；
- memo 重审的**体验类新发现**共 20 条已汇总在 `review-deep-memo-ui-report.md`，您已批复（12 项执行上线、8 项不做）；
- 登记在案的小残款（如 hover 能力判定两域各一份待上提 core 单源）不影响使用，台账在 `review-deep-bugs.md` 收官总账尾部，等真实触碰时再清。

---

*事实明细以 `review-deep-bugs.md`（逐域台账与收官总账）及 `.scratch/review-deep/` 下各方向报告为准；体验类清单见 `review-deep-ui-pending.md` 与其呈报版 `review-deep-ui-pending-report.md`。*
