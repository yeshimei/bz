# 包仔插件 · 全域深审修复总结报告

> 2026-09-19 · 覆盖范围：core（共享地基）+ 日记本 + 备忘录 + 剪藏本，共 4 个域
> 状态：**四域已全部修复上线**；其余 17 个域暂未开始（按您的指示暂停，随时可续）

---

## 一句话总结

对插件的 4 个核心域做了一次「地毯式体检」：**5 个审查视角并行扫描 → 发现问题当天修复 → 每个修复都配回归测试 → 全部通过后发布**。四轮下来共修复 **约 180 个问题**（含一批拖了一个月的旧账），回归测试从 5780 例增加到 **6207 例**，全部通过。

---

## 这次是怎么干活的

每个域走同一套流程：

1. **五路并行深查**：功能正确性、UI 缺陷、操作效率、风格一致性、架构与测试覆盖，各派一个审查代理同时扫。
2. **汇总拍板**：去掉重复项，按风险排序；拿不准的事按「低风险优先」做决定并记录理由。
3. **五路并行修复**：按文件分工，互不干扰，每人修完必须自带回归测试。
4. **串行合并发布**：逐批合并进主分支，全量测试通过后才构建部署。

---

## 各域修了什么（说人话版）

### 🧱 core — 所有功能共用的地基（修复 60+ 项）

地基有问题，家家都遭殃。这一轮把最底层的一批「老毛病」根治了：

- **弹窗关错东西**：开着弹窗按 ESC，有时候关掉的却是整个面板，留下一个关不掉的残影。现在每一层弹窗都能被 ESC 正确地逐层关闭。
- **回车按到危险按钮**：删除确认框里按回车，焦点有时落在「删除」上——一秒手滑数据就没了。现在确认框的回车永远落在安全的一侧。
- **设置改完悄悄丢**：快速改设置再关面板，最后那笔改动可能没保存。现在关面板前会先把改动落盘。
- **手机点按没反应**：一批按钮的触摸热区因为样式重构失效了，点了像坏了一样。已修复。
- **统一了一批「方言」**：时间显示、保存失败提示、删除通知等 30 多处各写各的重复代码，收编成单一来源——以后改一处全域生效。

### 📔 日记本（修复 50+ 项）

- **改标签会静默失败**：给加密日记改分类时，如果保险箱恰好自动上锁了，点「保存」弹窗关了、标签其实没改、也没有任何提示。现在会明确告诉你「改标签失败」。
- **写日记弹窗按 ESC 整面消失**：写了一半的内容直接丢。现在 ESC 只关弹窗，真有内容还会问你「要放弃吗」。
- **大图被抽屉挡住**：手机上长按条目看缩略图想放大，结果灯箱被抽屉盖住，点了像没反应。已修。
- **时间标签会漂移**：影视/信件条目的时间取自「文件创建时间」，复制或迁移文件后时间就变了。信件现在优先读正文里写的时间。
- **写日记更顺手**：弹窗支持回车直接提交；40 个标签可以输入即时过滤、常用的排前面；保存按钮固定在底部不再被顶出屏幕；日期行新增「此刻 / 昨天」快捷chip——补写昨晚的日记一步到位。
- **墙更好用**：搜索不再越搜越卡（改成只切换显隐），命中词高亮；筛选了某个月后标题会显示「2026-06 · 37 条」并给一枚「✕ 清除」；加密日记前会先二次确认（此前滑错菜单项日记就直接消失）。

### 📝 备忘录（修复 40+ 项）

- **「每月 31 号」会变成「每月 28 号」**：带重复周期的备忘录只要被编辑过一次（哪怕只改了标题），锚定日就被悄悄剥掉，之后永远按 28 号提醒。已修。
- **iPhone 上「延后一天」会写出坏日期**：日期算术用了 iOS 不认识的写法，延后直接落成 `NaN-NaN-NaN` 的脏数据，提醒从此失灵。已改用统一的时间库。
- **双击保存存两条**：保存按钮在慢盘写入窗口期没有防连点，双击就出现两条一模一样的备忘录。已加防重入。
- **编辑到一半全丢**：备忘录编辑器是全插件字段最多的表单，点一下遮罩或 ESC 就全部蒸发，连问都不问。现在有「放弃修改？」确认。
- **删除不再多问一次**：既然撤销按钮就在通知里，先弹「确定删除吗」纯属多打断一次。现在直接删、通知里一键撤销（跟收藏夹、归物本口径统一）。
- **搜索按 ESC 不再整面板消失**：现在 ESC 只清关键词；搜索框加了「✕」一键清除；命中词高亮；打开面板光标直接落在输入框。
- **读盘失败不再「看起来全没了」**：以前文件被同步盘锁住时面板一片空白毫无解释；现在显示「备忘录加载失败 + 原因 + 重试按钮」。
- **新语音**：「已完成」区新增一键「清理更早」，不用再逐条右键删十轮。

### ✂️ 剪藏本（修复 40+ 项，外加清掉 12 条一个月前的旧账）

- **全插件最后一个手搓确认弹窗退休**：重剪同名的网页时弹的那个白框，现在换成了统一的确认框，并且——第一次告诉你「覆盖会冲掉 AI 摘要、标签和手工编辑」，还新增了第三个选择「**另存为新剪藏**」（自动加序号，两篇都保留）。
- **面板被盖住「命令像失灵」**：剪藏本面板是全插件唯一没有动态层深的，先开收藏夹再开剪藏本会被压在底下。这类「面板压面板」的问题已根治（连阅读报告弹层一起）。
- **同一个词锚定两个笔记会产生坏链接**：划词存第二个名词时，会生成 `[[A|[[B|词]]]]` 这种嵌套破链，还会污染保存下来的剪藏文件。已加守卫。
- **j / k 键盘阅读突然失灵**：用鼠标点了目录条目后焦点不在阅读区，核心快捷键静默失效。现在点击后焦点自动跟上；Ctrl+K 这类组合键也不再被劫持成「切篇」。
- **剪藏「凭空蒸发」有诊断了**：浏览器扩展剪进来的网页如果缺字段，之前从面板上无声消失；现在会告诉你「目录里还有 N 篇无法识别」。
- **删除口径对齐**：跟备忘录同款，去掉多余的确认框，撤销兜底。
- **写盘失败不再假成功**：以前磁盘异常时「标已读」显示成功、实际没写上，重开全弹回来。现在会立刻报错。
- **搜索体验对齐备忘录**：ESC 只清词、✕ 一键清除、命中高亮、子任务和链接也能搜到；读完的 UP 主不会再从导航栏消失；阅读报告里「读得最久」的文章可以点击直接回看。

---

## 数字总账

| 域 | 审查发现 | 修复 | 回归测试净增 |
|---|---|---|---|
| core 共享层 | 63 | 60+ | — |
| 日记本 | 约 70（含旧账） | 50+ | +211 |
| 备忘录 | 55（去重后） | 40+ | +106 |
| 剪藏本 | 66（含 12 条旧账） | 40+ | +110 |
| **合计** | **约 250 项发现** | **约 190 项修复** | **测试 5780 → 6207（+427，全绿）** |

四轮全部以「全量测试通过 → 构建部署」收尾，每个数字都可复核（明细见 `review-deep-bugs.md` / `review-deep-ux.md` 两份台账与 `.scratch/review-deep/` 下 20 份方向报告）。

---

## 特意没修的（登记在案，非遗漏）

- **性能类**：日记写一笔全库重读（有防抖缓解，等真实卡顿再立项）；备忘录滑动手势、剪藏滑动切篇（需要真机试手感，不宜远程拍板）。
- **需要您拍板的**：B站风控提示原来指向的 Cookie 设置入口（此前您拍板移除过，本轮只把提示文案改成如实描述「将自动重试」；要不要在 UP 管理里加回 Cookie 输入，等您定）；覆盖同名剪藏时是否保留 AI 摘要做字段级合并（本轮只做了披露提醒）。
- **技术债**：部分老代码的内联样式、写链路的猴子补丁——无当下危害，登记渐进消化。

---

## 剩下的 17 个域（已暂停，待您指示）

复习、保险箱、密码本、影院、归物本、书库、游戏架、收藏夹、番茄钟、首页、设置面板、阅读报告、体检、自动摘要、附件、recap（退役域仅标注）。

其中两个已有「记名待办」：**体检域**要顺手收编一份重复的路径读取代码；**自动摘要域**背着 3 条旧账（其中 1 条 P2：frontmatter 反斜杠不转义）。

续做时说一声「继续逐域深审」即可从复习域接着跑，流程与本次完全一致。
