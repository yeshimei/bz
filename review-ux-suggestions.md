# 全域用户体验建议清单

> 2026-09-18 · 覆盖 21 个功能域 · 共 39 条



---

## 怎么用这份清单

- **每条一个编号**，你直接回「采纳 3、7、12」就行，我按号去做。
- 每条的**星级**是我对「值不值得做」的判断，不是难度：
  - ★★★ 强烈建议：经常用得上，或者关系到数据安全
  - ★★ 值得做：用起来会明显顺一点
  - ★ 锦上添花：现在也能忍，顺手做
- 每条的**成本**是实现的活量：**S** = 小改（一两处）、**M** = 中等（一个域内的一批改动）、**L** = 大改（涉及数据结构或跨域）。

### 已经帮你排除掉的（不用重看）

你项目里已经有一份 `issues/144-ux-batch.md`（全站 UX 批次），还有近期的 353~377 一串 issue。凡是**那里已经交付、或者你已经拍板要做的**，这份清单里都不再重复。例如：

- 收藏本/复习/测验/第二大脑的「删除可撤销」——已交付
- 影视、备忘录、归物本、密码的「删除可撤销 / 写盘失败提示 / 脏表单拦截」——144 里已拍板待做
- 番茄钟「专注中重置加确认」——144 里已拍板待做
- 密码剪贴板 60 秒自动清空——你已拍板要**去掉**，所以本清单不会反着提
- 回顾（recap）面板——已按 ADR-0157 退役，不再提
- 游戏库关面板会中止后台回填——你 2026-09-18 说过「不用改了，慢慢跑吧」，不再提

---

## 一、跨域通用（改一处，好几个功能都受益）

**1. 一个「全域搜索」入口，别让我记住东西存在哪个盒子里** ★★★ 成本 L

现在：想找「去年买的那个键盘」得先想起它在归物本；想找「那篇讲 RAG 的剪藏」得先想起在剪藏本。21 个域各自为政，跨域只能靠人脑。  
建议：给一个统一搜索，输入关键词后按域分组出结果（归物本 2 条 / 剪藏本 5 条 / 日记 1 条…），点一条直接跳过去。轻量做法可以复用第二大脑已有的检索索引，或先做一个「只搜标题」的简版。  
为什么值得：这是个人知识库最核心的一件事，也是现在唯一完全缺失的能力。

**2. 全站缺一套「多选 + 批量」** ★★★ 成本 M

现在：备忘录、收藏本、剪藏本、影院、归物本、书库**全都没有多选**，删十条内容就要点十次、确认十次。  
建议：给列表加一个「多选模式」（长按/勾选进入），选完统一做批量删除 / 批量归档 / 批量改状态。先挑你用得最多的两个域做，验证顺手了再铺开。  
为什么值得：这是 6 个域的同一个坑，一次做成共享组件，后续域直接接。

**3. 长任务要有「正在跑」的可见反馈，别让我猜** ★★★ 成本 M

现在：剪藏本抓取、游戏库同步、影院豆瓣补全、知识盒生成、第二大脑问答——全是「点完就没动静，等一会儿突然出结果」，中途不知道在跑还是卡死了。  
建议：统一一个进度提示（能报「正在抓 3/8 个源」最好，最低也要有转圈 + 一句「正在同步，可继续用其他功能」）。  
为什么值得：涉及 5 个域，且都属于「等的时候最焦虑」的场景。

**4. 把「内容 → 知识 → 复习」这条链子打通** ★★★ 成本 M

现在：一条剪藏、一条收藏、一篇日记、一段第二大脑的答案，都**没办法一键变成知识盒的文献笔记，也没办法一键变成复习卡**。必须先去打开原文，再跑去命令面板敲命令。  
建议：在这些地方的行内菜单加两个动作——「存为文献笔记」「加入复习计划」，复用知识盒/复习已有的弹窗，标题自动填好。  
为什么值得：这是你整套系统的价值主线（收集 → 消化 → 记住），断在这里最可惜。

**5. 默认快捷键：全域命令一个默认热键都没有** ★★ 成本 S

现在：`src/main.ts` 里注册了 60 多条命令，**没有一条带默认热键**，全靠你去「设置 → 快捷键」里手绑。番茄钟只有在弹窗聚焦时按空格才管用。  
建议：给最高频的几条配上默认键（如「加备忘录」「写日记」「开始/停止专注」「快速取密」），其余保持空着让你自己绑。  
为什么值得：改动极小，日常收益每天都在发生。

**6. 日记、书库、影院的「删了不能反悔」补上** ★★ 成本 M

现在：日记删除、书库删书、影院删条目都是删了就删了。删除可撤销这套机制你在 144 里已经给影视/备忘/归物/密码拍板了，但日记、书库没列进去。  
建议：照 144 的做法（`notifyUndo` + 数据层恢复）补齐这几处。  
为什么值得：一致性——同样是删除，有的能撤销有的不能，用久了会误判。

**7. 数据的「一键备份 / 导出」** ★★ 成本 M

现在：全域数据散在十几个 json（memo.json、belongings.json、favorites.json、review.json、password-vault…）+ 各种 md 目录里，插件内**没有任何导出或打包备份**入口，只有「文件坏了」之后的留档。万一误删或同步冲突，没有兜底。  
建议：体检面板加一个「导出全部数据（打包 zip）」，或至少提供「导出为 Markdown/JSON」。  
为什么值得：你现在的兜底完全依赖 vault 本身有没有 Git/网盘同步——如果已经有了，这条可以降级甚至不做。

---

## 二、数据安全（保险库 / 密码本 / 数据体检）

**8. 保险库默认不自动上锁** ★★ 成本 S ⚠️

现在：安全模式默认是**关**的（`settings.ts:682`、`784`），解锁后保险库就一直开着，不会自己锁；开了安全模式才是「15 分钟无操作上锁」，而且这个 15 分钟不可调。  
建议：解锁时给一句提示「要不要开自动上锁？」，并把时长做成档位（关闭 / 1 / 5 / 15 / 30 分钟）。移动端可以考虑「切后台就锁」。  
为什么值得：这是本清单里唯一直接关系到「密码被看到」的项。也可能你就是刻意关掉的——那这条跳过。

**9. 忘记主密码时，没有任何出口和说明** ★★ 成本 S ⚠️

现在：密码输错了就一直是「密码错误」（`encrypt/ui.ts:1210`），没有「忘记密码？」的入口；真正能清空数据重来的「重设」藏在「清单损坏」那个分支里（`ui.ts:1182-1208`），点错就是全库不可恢复。  
建议：解锁界面加一行「忘记密码？」——点开只说清两件事：一是**无法找回**，二是「建议在密码本里另存一份主密码 / 导出备份」。重设入口保持深藏。  
为什么值得：让人知道「这条路走不通」，比让人反复试密码要好。

**10. 两个「锁定」命令其实锁的是同一把锁，提示却说成两件事** ★★ 成本 S

现在：命令面板里「锁定保险库」和「锁定密码本」是两个命令，但底层调的是同一个 `lockSafe`，锁的是整个库；通知却分别说「保险库已锁定」「密码本已锁定」，容易让人以为它们各自独立。  
建议：统一成「保险库与密码本已锁定」，或干脆合成一个命令。  
为什么值得：消除一个会让人误判安全状态的假象。

**11. 销毁加密笔记没有撤销** ★★ 成本 S ⚠️

现在：销毁加密笔记要重新输主密码确认（这点做得对），但销毁完只有一条 toast，密文**永久没了**，手滑无法挽回。  
建议：销毁后的通知带一个「撤销」，给几秒后悔时间（数据层保留到窗口结束再真删）。  
为什么值得：这是全域最不可逆的操作。

**12. 密码本没有批量导入/导出** ★★★ 成本 M

现在：只能一条一条手填（`password-vault/ui.ts:931`）。要是从浏览器或别的密码管理器迁过来，等于重打一遍。  
建议：支持导入 CSV / JSON（给字段映射预览再确认写入）+ 导出备份。  
为什么值得：迁移成本高到会让人直接放弃用这个域。

**13. 数据体检要手动跑，红色问题不能一键修** ★★ 成本 M

现在：体检得自己想起来去点（`checkup/ui.ts:129`）；最严重的「红色：json 坏掉 / 结构异常」只给「查看详情」，不给修法，你得自己摸。  
建议：① 给个「打开保险库后自动跑一次轻体检」的选项；② 红色项详情里写清**具体怎么救**（哪份留档、放回哪里）。  
为什么值得：体检的价值全在「发现问题之后能怎么办」。

---

## 三、日常记录（日记 / 备忘录 / 收藏本 / 归物本）

**14. 日记卡片单击没反应，必须双击** ★★ 成本 S

现在：桌面端单击日记卡什么都不发生（`diary/ui.ts:879-888`），要在 300ms 内双击才跳原文；移动端单击同样没反应，只能长按（`ui.ts:890-899`）。第一次用会以为卡了。  
建议：桌面单击就给轻反馈（高亮一下 + 提示「双击打开」），移动端改成单击直接出抽屉、长按做次级动作。  
为什么值得：这是日记域每天的第一下操作。

**15. 备忘录每次打开都退回列表视图** ★ 成本 S

现在：你切到月历视图，关掉面板再打开，又回到列表（`memo/ui.ts:330`）。  
建议：记住上次的视图，或者给个「默认视图」设置项。

**16. 收藏本：桌面端所有操作都藏在右键里** ★★ 成本 M

现在：编辑 / 置顶 / 归档 / 删除**全部只在右键菜单**（`favorites/ui.ts:240-246`），界面上一个按钮都没有，不右键就找不到。  
建议：卡片常驻一个「⋯」按钮（移动端本来就靠长按，保持一致），鼠标悬停时也浮出操作。  
为什么值得：右键是隐藏技能，界面上得有个看得见的入口。

**17. 收藏本：没有链接的卡片点了毫无反应** ★★ 成本 S

现在：点击没有链接的收藏卡，既不打开也不提示（`favorites/ui.ts:236-239`），看起来像坏了。  
建议：给一句「这条收藏没有链接」，或者干脆改成点击打开编辑。

**18. 归物本：没有批量导入，只能一条条记** ★★ 成本 M

现在：工具栏只有「记一笔」（`belongings/ui.ts:9-13`），从账单或截图一次性登记十件物品，得开十次弹窗。  
建议：支持多行文本粘贴 / CSV 导入（「名称, 价格, 日期」这样的格式，解析后预览再写入）。  
为什么值得：和 12 号是同一类问题——录入效率决定你还愿不愿意记。

**19. 归物本：筛选里没有「按分类」** ★★ 成本 S

现在：只能按状态（四态）+ 年份 + 搜索筛，分类图标已经渲染出来了，却不能拿它筛。  
建议：加一个分类维度的筛选或分组。

---

## 四、内容消化（剪藏本 / 书库 / 影院 / 游戏库 / 报告）

**20. 剪藏本：点开一条就自动标已读，想反悔没门** ★★ 成本 S

现在：打开一条未读就**静默**标记已读，没有提示也没有撤销（`clipbook/ui.ts:1420-1438`）——这是当初有意设计的（ADR-0108）。但「只想瞄一眼」的场景下，未读状态就没了。  
建议：给打开后一个几秒的「撤销（标回未读）」，或者区分「已展开」和「已读」两个状态。  
为什么值得：你的未读流是「待处理队列」，队列位置不该被一次误点吃掉。

**21. 书库：面板里删不掉书** ★★ 成本 S

现在：书籍卡片只能看，删书必须退出到 Obsidian 文件管理器（`bookshelf/ui.ts:154` 附近无删除入口）。  
建议：卡片菜单加「删除书籍」+ 二次确认（走回收站）。  
为什么值得：一个域的列表页没有删除入口，总会让人来回切窗口。

**22. 书库：读书笔记划线删了不可恢复** ★ 成本 S

现在：删划线时的文案明确写着「此操作不可撤销」（`bookshelf/notes-ui.ts:117`）。  
建议：改成进回收站或给撤销。

**23. 阅读报告：翻完不知道点哪回书架** ★ 成本 S

现在：报告视图里没有常驻的「返回书架」，只有空态和点作者/分类时才有返回路径（`reading-report/index.ts:50-52`）。  
建议：报告页头行常驻一个返回入口。

**24. 阅读报告：没数据时不说清「还差什么」** ★ 成本 S

现在：报告靠 `书库/*.md` 的 book 标签和 EPUB 数据生成，条件不满足时只给一句空态，你得自己猜为什么是空的（`reading-report/index.ts:95-99`）。  
建议：空态直接列条件，比如「需要 3 本以上带 book 标签的笔记，当前 0 本」。

**25. 游戏库：没配 Steam 时，同步按钮是灰的，也不说为什么** ★★ 成本 S

现在：「立即同步」在没配置时直接置灰、点不动、无解释（`gameshelf/ui.ts:781`），引导只在面板正文里。  
建议：灰按钮也可以点——点了直接跳到 Steam 配置那一项。  
为什么值得：不可点的按钮不解释原因，是最容易让人卡住的设计。

---

## 五、知识体系（知识盒 / 复习 / 第二大脑）

**26. 知识盒：段落/图版重名时，静默生成一个「\_2」副本** ★★ 成本 M

现在：名词重名会在写之前拦住你（`knowledge/ui.ts:3396`），但段落和图版是悄悄改名叫 `xxx_2` 存下来，你根本不知道多了一篇。  
建议：统一在生成前提醒重名，给「覆盖 / 新建副本 / 合并」三选。  
为什么值得：重复内容积累起来，后面挂载树和检索都会变脏。

**27. 知识盒：生成完自动跳走，没法连着录** ★ 成本 S

现在：确认写入后自动打开新笔记（`knowledge/ui.ts:3452`，issue 326 拍板）。单条录入很顺，但连录十条就被打断十次。  
建议：给一个「生成后留在面板继续录」的开关（默认关，保持现在的行为）。

**28. 知识盒：影像录入是另一套界面** ★ 成本 L

现在：名词/段落/图版走同一套录入壳，只有「影像」是独立对话框（`knowledge/ui.ts:3507`），交互范式不统一，命令名也容易和「图版」混。  
建议：影像并入同一套壳；命令名统一成「录名词 / 录段落 / 录图版 / 录影像」。

**29. 知识盒：关联候选点掉就找不回来了** ★ 成本 S

现在：候选 chip 点掉不可恢复（`knowledge/ui.ts:2484-2490`），只有重新生成才可能回来（ADR-0152 决策 18 明确不给恢复）。  
建议：给点掉的 chip 一个「本轮内加回」。

**30. 知识盒：挂载树要先打开一张笔记，手机上更难看** ★ 成本 M

现在：「看挂载树」必须先有活动笔记（`main.ts:199`），没有就只给提示；移动端虽然有适配，但大画布在小屏上很挤。  
建议：没有活动笔记时给「选一篇笔记」的入口；移动端提供列表/缩进树的替代视图。

**31. 复习：卡片只显示标题，复习前得先点开原文才知道是什么** ★★ 成本 M

现在：队列里只有标题 + R 值 + 日期（`review/ui.ts:400`），要判断「这条我会不会」得先打开笔记再返回，来回折腾。  
建议：卡片抽屉里放一段正文摘要预览（复用已有的正文读取），不用离开面板就能回顾。

**32. 第二大脑：手机上提问后只有一个变灰的按钮** ★★ 成本 S

现在：移动端发送后只是把按钮禁用（`secondbrain/mobile-panel.ts:440`），没有「思考中」之类的话；桌面端是有流式气泡的（`chat-panel.ts:257`），两边不一致。  
建议：移动端也显示「思考中…」占位，和桌面同款。

**33. 第二大脑：向量索引不可用、降级成文本匹配时没提示** ★ 成本 S

现在：索引不可达时会降级为文本匹配（结果质量明显下降），只有面板底部一行状态（`secondbrain/panel.ts:490`），检索的时候看不出来。  
建议：降级时在结果区顶部提示一句「当前为文本匹配（向量索引不可用）」。

---

## 六、入口与设置（首页 / 设置面板 / 番茄钟 / 小橘）

**34. 首页：15 个磁贴平铺，没有搜索也没有分组** ★★★ 成本 M

现在：首页就是 15 个入口平铺（`home/shared.ts:42-65`），插件里没有搜索框（`home/ui.ts` 里搜不到 search）。  
建议：加一行搜索（输入「番」筛出番茄钟/剪藏）；或按「内容 / 工具 / 安全」分组；再给一个「最近使用」区。  
为什么值得：首页是全域最常走的那扇门，这条和第 1 条是配套的。

**35. 首页：手机上要滑过两段才看得到功能入口** ★★ 成本 S

现在：移动端首页顺序是「时间线 → 明天预告 → 全部域瓦片」（`home/ui.ts:7` 注释），想进任意功能都得先滑两屏。  
建议：移动端顶部加一行常驻的域快捷入口，或把瓦片提到时间线前面。

**36. 首页：调整入口顺序得先去设置面板** ★ 成本 S

现在：改首页顺序/隐藏入口藏在「设置 → 首页入口」弹窗里（`home/shared.ts:15` 注释）。  
建议：首页自己给一个「编辑布局」入口。

**37. 设置面板：没有「全部恢复默认」，而且有些设置项重置不了** ★★ 成本 M

现在：只能逐域「重置本域」，而且只重置带字符串键的行——三函数绑定的行会被跳过（`settings-panel/ui.ts:578-620`）。  
建议：加一个总的重置入口，并让三函数行也有明确的重置路径。

**38. 设置面板：桌面端搜到别的域的设置项，点不进去** ★ 成本 S

现在：桌面搜索命中了其他域的项目，只高亮当前域、不跳过去（`settings-panel/ui.ts:381-387`）；移动端是能跳的（`pushDomain` 带 `focusRow`），两端行为不一致。  
建议：桌面也支持点中跨域结果直接定位到那一行。

**39. 小橘：设置靠「长按猫本体」打开，没人知道这个手势** ★ 成本 S

现在：设置统一由长按小橘打开（`smartcat/ui.ts:7` 注释），首次没有任何提示。  
建议：第一次出现时给一句气泡「长按我可以改设置」。

---

## 附：我确认过、**不建议改**的（免得你以为是漏了）

| 项             | 为什么不动                                           |
| ------------- | ----------------------------------------------- |
| 知识盒「生成即开笔记」   | issue 326 你拍板的设计，剪藏本工具框流程里已经用 `onCreated` 避开跳转了 |
| 剪藏「打开即已读」     | ADR-0108 有意设计，我只建议加撤销窗口（第 20 条），不推翻机制           |
| 回顾（recap）面板   | ADR-0157 已退役，数据层保留给首页消费                         |
| 游戏库关面板中止回填    | 你 2026-09-18 拍板「不用改了，慢慢跑吧」                      |
| 保险库 26 个外观占位键 | ADR-0159 有意保留，等皮肤扩展                             |
| 密码剪贴板自动清空     | 你已拍板要去掉，本清单不会反着提                                |

---

## 优先级速览

| 档位       | 编号                                                        |
| -------- | --------------------------------------------------------- |
| ★★★ 强烈建议 | 1、2、3、4、12、34                                             |
| ★★ 值得做   | 5、6、7、8、9、10、11、13、14、16、17、18、19、20、21、25、26、31、32、35、37 |
| ★ 锦上添花   | 15、22、23、24、27、28、29、30、33、36、38、39                       |

（第 27、28、29、30 条虽然标 ★，但都在知识盒域内、彼此相关，可以打包一起做。）
