# 全域体验增强审查报告（除 smartcat / secondbrain / literature）

> 审查日期：2026-09-04。与 `review-all-domains-bugs.md` 同一批子代理产出（12 个只读子代理，6 组 × bug/体验两线）。只谈体验增强，不报 bug；已对齐设计手册/组件库手册与 CONTEXT.md 已拍板决策。
> 说明：审查期间仓库合入了 bookshelf B1-B11（d061df1）与 todo T1-T6（4a9f17b）修复，相关结论基于合并后代码。

## 问答决策（2026-09-04，逐项拍板）

**✅ 做**：影院右键/长按菜单、AI 页闭环（转圈/换一批/已在库中）、空态带按钮、删除进回收站+豆瓣直达+AI 卡跳豆瓣；报告页现代化（lucide/桌面去关闭钮/空态动作/头行小计）；首页钉选排序+小包（统计格可点/聚焦搜索/30s 刷新/补文献盒与读书报告入口）；自动摘要手动重跑+批量排队聚合；回忆墙灯箱连看/章节栏年份分组/小包（计数口径/lucide/去文件名/在日记本中查看）；搬附件升级包（撤销/前置数量/进度/可勾选预览）；保险库快速取密+复制密码命令/表单效率（强弱提示/查重/回车流转）/防偷看（默认打码+15s 回遮）/状态真实化（体检反映到概览左栏/解锁时长/15 分钟自动上锁/emoji 清理）；剪藏本桌面搜索/移动长按/右栏读正文/容错包（回收站+5s 撤销+全部标已读）/阅读动线（自动在读/处理后下一篇/键盘切换）；书架打开原文·继续读+在读卡可点/手机排序/状态保存撤销/分类筛选/读书纪念日卡；读书报告图表升级（三环形改条形/热力图翻月/年度下钻/emoji 清理）；收藏归档可找回（撤销+已归档入口）/小包（搜网址/贴链自动进链接框/域名徽章）；归物容错包（流转与删除撤销/默认展开近两年/统计卡可点筛选）；待办删除撤销/场景管理闭环（右键重命名·删除·直达设置+「重要」视图）/录入改进（toast 补全按钮/已完成默认近 30 天）；复习冲刺键盘化/答错一行解析/普通模式评级条/队列体验包（今天到期列口径/跳过此篇/清零变绿/结算连续天数）/一致性小包（R 口径统一/累计次数真实化/统计弹窗清理）；番茄钟完成通知带动作/时段分布条形图+今日分钟数/循环进度方点。

**❌ 不做**：日记域全部建议（旧域不投资）；备忘录收尾包（旧域）；读书报告分节导航；收藏批量打标；启动器全部（幽灵磁贴清理+编辑可发现性包）。

**🔧 改法调整**：复习队列搜索框不补 `/` 聚焦快捷键——改为去掉 `/` kbd 图标提示，换成与其他域一致的普通搜索框。

**⏳ 补问进度**（初次问答未覆盖及修复线转来的拍板项，逐个补问中）：

- ✅ 待办×番茄联动：待办条目「专注这个」直接开番茄并记录归属——**做**
- ❌ 保险库加密备份导出——**先不做**
- ❌ 编辑器选中文本一键建待办/备忘——**先不做**
- ✅ 剪藏本阅读字号可调（小/中/大记住选择）——**做**
- ✅ 剪藏本面板拖拽缩放+尺寸记忆（对齐待办）——**做**
- ✅ 密码平台头像保持外取，但修清晰度——**做**（用户反馈：缓存图都不清晰；修法：`core/dom.ts:101` yandex 取图未传尺寸参数导致小图放大发糊，改带尺寸的高清源/参数，且 localStorage 缓存键升级使旧糊图自动失效）
- ✅ 影院内嵌分析页配色正规化（定义变量+与独立报告页统一+暗色适配）——**做**
- ✅ 报告打开期间自动刷新——**做**；**并升级拍板：报告全面内嵌化**——影视分析报告不再独立弹窗（影院面板已有内嵌分析页，独立窗内容并入/退役，命令入口改直达内嵌页）；读书报告同样嵌进书架墙面板（左栏报告入口改为面板内切换到报告视图）。原「报告页现代化」「图表升级包」「报告→书架深链」均并入内嵌版一体实现（深链在同面板内变为点作者/分类直接筛列表）。
- ✅ 番茄钟视觉小包（🍅 换线条图标/遮罩色对齐/可选空格键开始暂停）——**做**
- ❌ 归物年份筛选平铺化——**先不做**（维持下拉）
- ✅ 读书报告统计范围：**只算书库目录**（对齐书架墙口径，库外 book 标签笔记不再混入报告）
- ✅ 复习调度拍板：**满血 FSRS**——阶段爬满 9 级后不再固定 120 天循环（也不再重置记忆参数），交由 FSRS 按每次评分动态算间隔
- ✅ 待办「今日」视图口径：**只看今天**（已完成区仅显示今天完成的含今日补完的逾期项；历史完成去「全部」场景看，两处计数对齐）

**补问结束（2026-09-04）**：15 项全部拍板——9 做、6 不做。做项汇总：待办×番茄联动、剪藏字号可调、剪藏面板可缩放、密码头像修清晰度（保持外取+高清源+缓存键升级）、影院内嵌分析页配色正规化、报告自动刷新+报告全面内嵌化（影视报告并入影院面板/读书报告嵌入书架墙，独立弹窗退役）、番茄钟视觉小包、读书报告只算书库目录、复习满血 FSRS、待办今日只看今天。

## UI 细节轮（2026-09-04，第二轮问答）

UI 表现层专项审查（2 个只读子代理扫 15 域 styles.css）发现约 30 处细节问题，归并 16 个决定逐批拍板：

- ✅ **手机触控热区统一加大**（待办勾选框 19px、剪藏来源条/保存钮、首页小入口、影院状态灰字、回忆墙横滑标签）——**做**；**追加要求：移动端全屏面板离顶部留 44px 边距**
- ✅ **头行图标钮间距/尺寸**（favorites/belongings/review/bookshelf 头行 2~4px 间距→≥8px，手机换大按钮档）——**做**
- ❌ 键盘焦点伞规则（core focus-visible + 保险库输入框/归物下拉焦点）——**先不做**
- ✅ **无悬停反馈的可点元素**（library 筛选胶囊/按钮/关闭叉、belongings 年份折叠头、pomodoro 状态栏图标）——**做**；**追加：全站界面 emoji 全部改线条图标**（数据语义 emoji 如日记条目标签保留）
- ✅ 保险库风险警告框亮黄死色→主题警示色；保险库 `--background-modifier` 写错变量名致六处样式失效→改正；保险库金色渐变浅端白字对比 1.5:1→浅端加深/深金字 ——**三项均做**
- ❌ 旧书库（library）状态徽章死色+rem 混排——**不做（旧域）**
- ✅ 组件库扩修饰符救回被 `!important` 压制的三处设计（影院 AI 卡「加入想看」钮、详情类型/想看彩色徽标、首页编辑模式开关）——**做**
- ✅ 复习冲刺对错标记 ✓✕ 文本符号→lucide 线条图标；难度弹窗 easy 档 `#7bed9f` 死色→主题变量——**做**
- ✅ 过小字号统一抬到 11px（书架柱图 9px、各域计数徽标/队列标签/剪藏角标 10px 等）——**做**
- ✅ 字重 800→700、半像素字号（13.5/12.5/11.5/10.5）并入标准档（保险库为主，归物/复习零星）——**做**
- ✅ 回忆墙右键菜单 z-index:2000 静态档被墙盖住→topifyZ 动态发号——**做**
- ✅ 静态层级清理：favorites 100000/110000、belongings 同、encrypt 弹窗 2200→动态发号——**做**
- ✅ 剪藏本移动端刘海屏 safe-area（顶栏避让+底部指示条）——**做**
- ✅ 杂项打磨包：影院/首页非档位间距控件高归档、首页统计胶囊 ellipsis、书架封面悬停阴影 token、复习队列滚动条隐藏、保险库/复习遮罩去毛玻璃、剪藏死选择器删除——**做**

**UI 细节轮结束**：16 个决定——14 做、2 不做（键盘焦点伞规则、旧书库徽章）。连同前两轮，累计拍板「做」约 61 项，**全部待实现（等用户指令后派子代理）**。

## 探索方向拍板（2026-09-04）

用户从探索建议中采纳两项（设计级 backlog，实现前需先出小设计）：

- **方向一·横向时间线**：①今日回顾页——按时间聚合当天日记/影视/书/待办/番茄痕迹，可一键生成总结写进日记；②生活周报——home 顶部跨域数字故事轻卡片（本周 N 部电影/N 本书/N 番茄/完成率）。
- **方向三·数据可靠层**：把本轮验证过的「读改写队列（enqueueFileTask）+ 段级合并（news write-queue 先例）+ 冲突留档」固化为 core/storage 统一契约，全部域强制走；附带全插件「数据体检」页（json 损坏/字段漂移/孤儿条目一键体检，仿保险库体检交互）。

未采纳（备忘）：方向二跨域语义检索、方向四复习素材扩展、方向六新域候选；方向五退役红利部分独立执行（见旧域删除流程）。

## 命令与设置轮（2026-09-04，第三轮问答）

命令面板/设置页专项审查 12 处发现，归并 4 包全部拍板**做**：
1. **设置面板修正包**：删孤儿键 `clippingMobileDefaultFullscreen`（实际生效的是 clipbook 键）；todo 移动端组去掉多余描述（对齐其余 14 域）；设置子项（`.bz-sp-set-row.child`）补透明度降级（层级拍板的后半段没落地）；书架「书库文件夹」空值时 chip 显示实际回落目录。
2. **命名与术语包**：「加待办（待办）」→「加待办」；设置面板「文献笔记」→「文献盒」（与命令面板/术语表统一）；日记本组「影视目录」（movieDirectory，仅日记本/回忆墙归类用）与影院组「影视文件夹」加区分说明。
3. **图标统一包**：两对重复图标错开（读书笔记/书架墙同用 book-open、两个分析报告同用 bar-chart-3）；设置面板导航域图标与命令面板（=入口页磁贴）同源为单一事实源。
4. **文案与默认值包**：域描述删「（新域）」「ADR-008x」开发黑话，只留功能语义；`secondBrainRemoteOllamaUrl` 默认值从写死内网 IP 改留空。

另登记不问：library 两条命令（bz-library-open/读书笔记）随退役票下架。

## 流程走查轮（2026-09-04，第四轮问答）

三个流程走查代理（捕获流/消费记录流/资产安全流）+ 文案口径审查。资产安全组已拍板：
- ✅ **收藏本流程小修**：表单防丢检查补全（标签/置顶/关联笔记纳入 baseline）；保存不再被余额查询阻塞（先落盘、后台刷新）。
- ✅ **收藏关联笔记选择器**：手输完整路径改输入时笔记候选自动补全。
- ✅ **归物出离闭环（含翻案）**：出离记日期、陪伴天数封口；转卖可选记售价、统计算回本——**推翻 ADR-0083「转卖不填价」拍板**；表单补防丢检查。
- ✅ **保险库流程三小修**：锁定时「加密当前笔记」改弹解锁后继续；还原冲突列具体路径；概览页搜索生效（自动切密码结果）。
- ✅ **回忆墙加密媒体按需解密**：解锁后墙上照片墙内解密直显原图（不再死占位）。
- ✅ **回忆墙流程小包**：上锁后加密分类状态实时归位（订阅 encrypt:unlock-changed）；年月跳转入口显式化（提为头部按钮）；跳原文后回墙恢复筛选与滚动位置。
- ✅ **术语统一包**：保险箱→保险库（墙侧 3 处）；阅读报告按钮统一「阅读分析报告」；影院空态指路改「影院」；home 备忘录统计改「N 条备忘」（「待办」让给 todo 域）；剪藏副题去「聚合讯」改「未读流与剪藏」。
- ✅ **确认框与按钮统一包**：删除确认全站三段式（标题「删除X」+问句+后果说明，名称引号统一「」）；确认按钮动词化；「移出复习计划」命令路径补撤销对齐面板口径。
- ✅ **错误通知人话化**：todo 七处/cinema 两处换 notifySaveError 统一模板；裸「已删除」toast 补对象名。
- ✅ **空态与时间格式包**：复习空库两条路引导（命令/监听文件夹+「把当前笔记加入」）；待办空态补新建指路；相对时间全站统一「N分钟前」无空格口径（pw-view 改调 core）。
- ✅ **待办捕获流三件**：启动弹出/打开笔记提醒/侧栏图标三被动入口改落 todo 面板（memo 接管迁移第 3 项提前实施）；「今日」视图录入当场可见+场景兜底改 memoDefaultScene；剪藏场景自动读剪贴板预填（复用 memo 同款 extractUrlAndDisplay+fetchPageTitle）。
- ✅ **剪藏与附件小修包**：B站保存分流后回写 state+「已转入文献盒」通知（防重复建任务）；AI 摘要完成通知带「查看」跳剪藏本选中该条；文件列表右键「搬移此笔记附件」（file-menu）；path-picker 确认键改「下一步」。
- ✅ **影院 AI 荐片按需触发**：入口只切页，有上次结果先展示，「开始/重新推荐」按钮页内化（与已拍板 AI 反馈闭环同页施工）。
- ✅ **观影/读完日期修正定式（两域）**：影院编辑表单加观影日期、书架详情已读状态可改完成日期（默认今天），补录不再污染统计。
- ✅ **影院/书架小修**：想看升级弹窗默认选「已看」；书架 md 书详情加「打开笔记」直达（与 library 迁移的划线回顾入口并存不冲突）。
- ❌ 书架「添加书目」表单——**维持 P1 原型拍板**（收录仍手建笔记，空态已有教程文案）。
- ✅ **「开始本轮」与「今日到期」对齐**：今日到期但时刻未到时给明确反馈或允许提前开始今天全部。
- ✅ **普通复习模式中断判定放宽**：离篇数分钟才算中断 + 本轮队列可恢复继续。
- ✅ **番茄钟休息结束文案按实况**（不再说「开始专注」却不计时）。
- ❌ 番茄×复习轻联动（专注完成带「去复习」/结算带番茄动作）——**先不做**（冲刺计入专注时长口径另议）。

**流程走查轮结束**：17 包决定——15 做、2 不做。连同前三轮，累计拍板「做」约 **82 项**（待实现）；「不做/维持」14 项。

## 跨域主题（多条建议的共同收敛点，建议作为专项票处理）

1. **撤销范式拉齐（notifyUndo + vault.trash）**：core `notifyUndo` 已有范式但只有 favorites/review 在用。缺位的高危动作：diary 删除（确认框写「不可撤销」）、attach 搬移、belongings 状态流转与删除、memo 删除、todo 删除、clipbook 标记已读（即刻删 body）/删除（不进回收站）、cinema 删除（真删）、bookshelf 状态保存（清掉已读日期无感丢失）。其中 clipbook 删剪藏笔记与 cinema 删除条目建议改 `vault.trash`（系统回收站），确认文案同步改「已移入回收站」。
2. **空态三件套统一**：favorites/belongings 已用 `.bz-empty`（图标+标题+动作），cinema/movie-report/todo/memo 仍是单行灰字或无动作。设计手册 §8.3 要求空态必须「一句话+一个动作」。
3. **lucide 收尾**：movie-report（19 板块 emoji 标题+❌ 关闭钮）、diary-wall（头行/右键菜单/抽屉符号）、memo（唯一全域残留 emoji 头行钮）、reading-report（🧮/❌/🏆）、pomodoro（🍅）、launcher（「文」/「✓ 完成」）、encrypt 残余（❌ 预览关闭、🎬🖼 附件 chip、★、⚠️）。
4. **移动端能力对齐**：clipbook 移动端条目无长按抽屉（全部行操作只有桌面右键才有，违反手册 §8.2「不要在移动端隐藏功能来简化」）；bookshelf 移动端无排序入口；home 移动端统计条不可点。
5. **键盘化收尾**：review 冲刺答题键盘化、launcher 命令选择器 ↑↓、home 打开自动聚焦搜索、pomodoro Space 开始/暂停。

## 全域优先级 Top 8（小成本×高价值）

| # | 建议 | 域 | 成本 |
|---|---|---|---|
| 1 | 影院海报卡统一右键菜单/长按抽屉（标记在看/已看、找同类、豆瓣打开全部一跳直达） | cinema | 中 |
| 2 | clipbook 桌面端搜索（融合时从旧 clipping 丢失的能力倒退） | clipbook | 小 |
| 3 | clipbook 移动端长按抽屉（动作构建器与桌面同源，一处接入） | clipbook | 小 |
| 4 | 保险库快速取密：解锁后直落密码资产+搜索聚焦；进阶 `bz-encrypt-copy-password` 免开面板复制 | encrypt | 小~中 |
| 5 | 复习冲刺键盘化（1-4/A-D 答题、Enter 推进；旧 quiz-core 已有实现可搬） | review | 小-中 |
| 6 | 番茄钟阶段完成通知带「开始休息/开始专注」动作按钮 | pomodoro | 小 |
| 7 | 自动摘要「重新生成摘要」手动入口（右键菜单 + 可选命令） | auto-summary | 中 |
| 8 | 撤销/回收站专项票（跨域主题 1 一次性拉齐） | 多域 | 中 |

---

# 一、影视与首页组（cinema / movie-report / home / auto-summary）

## 1. 影院 src/cinema/

**现状**：桌面左栏分类树 + 海报网格、主头行=筛选名+计数+添加、排序三档 segmented、AI 荐片页内化、影视分析 15 板块内嵌——范式基本齐整；短板在**卡片级高频操作入口**（右键/长按缺席、状态升级藏在灰色小字）与 AI 结果页的反馈闭环。

1. **海报卡统一右键菜单/长按抽屉（范式缺席，价值最高）**——影院是唯一没接 core 统一手势的内容域。高频动作埋得深：「想看→已看」要点卡片下 caption 级灰色小字，「找同类」要先开详情弹窗，「在豆瓣打开」要进详情滚到链接行。方案：复用 `core/item-actions.ts` 的 `attachItemActions`（先例：clipbook），桌面右键出菜单（打开详情/标记在看/标记已看/找同类/在豆瓣打开/编辑/删除），移动端长按出 `.bz-item-sheet` 同项。成本中，价值高。
2. **空态区分「库为空」与「筛选无结果」，都带动作**——现在空态只有文案没有按钮。`M.items.length===0` 走 `uiEmpty`（「还没有收藏的影视」+「添加第一部影视」）；筛选空时按钮改「清空筛选」。成本小，价值中高。
3. **AI 荐片反馈闭环**：等待页加 `.bz-spinner--lg`；结果页头部加「换一批」；已入库的推荐卡按钮置 check +「已在库中」禁用态（`M.items.some(i => i.name === rec.title)` 现成可比对）。成本小，价值中高。
4. **删除改为进回收站**：`vault.delete` → `vault.trash(item.file, true)`，确认副文案改「将移入回收站」。成本小，价值中。
5. **豆瓣直达**：详情弹窗 doubanUrl 行改「豆瓣页面」外链按钮；AI 推荐卡名旁加外链小图标（`douban.com/search?q=片名` 新窗打开）。成本小，价值中。
6. **（一致性）内嵌分析页与独立报告页视觉统一 + 暗色适配**：影院分析页引用的 `--bz-cinema-*` 变量没有任何 styles 定义（现状靠浏览器回退侥幸可看）。方案：在 cinema/styles.css 正式定义该组变量（暗/亮两套），两个分析文件共用同一套图表配色常量。成本中，价值中。

**已到位，勿动**：主头行范式、排序三档且移动端隐藏、AI 结果不弹窗（拍板）、左栏手风琴筛选模型、搜索已覆盖导演/主演。**不建议**加年份/导演筛选维度——搜索已覆盖，年份低频，加了徒增左栏噪音。

## 2. 影视分析报告 src/movie-report/

**现状**：19 板块统计完整、空库引导与移动全屏齐备；遗留是与影院内嵌版的视觉/范式漂移。

1. **lucide 化 + 关闭钮对齐范式**：19 个板块标题全带 emoji、关闭钮「❌」桌面常驻——影院内嵌版同款 `sectionHTML` 已是 lucide 参数化、桌面无关闭钮。照影院版加 `icon` 参数逐板块换；关闭钮改 `uiIconBtn` 仅移动端。窗口标题「📊 观影数据分析」为既有拍板文案可保留。成本小，价值中。
2. **空态带动作**：加主按钮「打开影院添加」（执行 `bz-cinema-open`）。成本小，价值中。
3. **头行补统计小计**：标题右侧加「N 部 · 已看 N · YYYY–YYYY」。成本小，价值低中。
4. **（可选）打开期间数据自动刷新**：照抄影院 `registerAutoRefresh` 先例，路径命中时重算只更新内容区。成本中，价值低中（低频场景排最后）。

## 3. 内容首页 src/home/

**现状**：问候+搜索+钉选卡+未钉 chips+各域一览，只读快照契约干净；摩擦在钉选不能排序、统计时效是打开瞬间、个别内容域没有曝光位。

1. **钉选卡片排序**：卡序=pinned 数组序但 UI 只能增删，想调序唯一途径是手编 json。方案：编辑模式每卡加 ←/→ 小钮（`movePinned(id, delta)`），不建议排序设置项。成本中，价值中高。
2. **移动端统计条可点**：三格统计条是纯展示，而侧栏在移动端隐藏——统计条正是移动端最自然的第二入口，点「复习到期 3」没反应。方案：三个 `bz-home-mstat` 加 `data-home-side` 复用侧栏点击路径。成本小，价值中。
3. **桌面打开自动聚焦搜索**：首页语义是「新标签页」，最高频动作是直接敲命令搜索。`createOverlay` 末尾桌面 `q.focus()`（移动端跳过避免弹软键盘）。成本小，价值中。
4. **运行中统计的时效性**：快照只取一次——番茄「剩 12:35」是冻结假倒计时。方案：overlay 存续期间 30s 轻量 interval 重拉快照重绘，`closeOverlay` 清理；不做秒级跳动。成本中，价值中。
5. **域清单补两个内容域 + 空钉选态可点**：DOMAINS 缺文献盒（`bz-secondbrain-panel`）与阅读报告（`bz-reading-report-open`）两个内容域的曝光位；空钉选提示改按钮点击进编辑。成本小，价值低中。

**已到位**：快照只读契约、徽标真实统计+高亮语义、搜索域+命令双源与键盘导航、编辑模式勾选视觉。

## 4. 自动摘要 src/auto-summary/

**现状**：监听触发、缺什么补什么、失败人话化+重试、写回前重读防覆盖——后台链路相当完善；唯一的大缺口是**没有任何手动重跑入口**，其次是批量场景无并发治理。

1. **「重新生成摘要」手动入口（价值最高）**：AI 摘要不满意/正文更新/字段被误删时，唯一办法是手删 frontmatter 字段再重开文件——这个弯路大部分用户发现不了。方案：`processFile` 加 `force` 参数（跳过 missing 检测；建议 force 档只重建 summary/tags，不动用户自定义标题）；入口挂剪藏本统一右键菜单/长按抽屉 + 可选命令 `bz-auto-summary-redo`。成本中，价值高。
2. **批量剪藏的串行队列与通知聚合**：一次保存多篇时并发发 N 个 AI 调用、叠 N 条 progress 通知。方案：pending 改 FIFO 队列（串行或限流 2），通知合并为「正在生成摘要 3/8…」。成本中，价值中。

**已到位**：失败人话化+通知内重试、rename 失败回退、写回前重读防并发覆盖、设置三项级联显示。

---

# 二、日记簇（diary / diary-wall / attach）

## 1. diary（日记本）

**现状**：标签/日期筛选+条目流+写日记弹窗，UI 处于 ADR-0081 兼容性冻结（以下建议均为增量挂载，不动既有 DOM 契约/数据格式）。

1. **删除接 core 撤销通知（价值高/成本小-中）**：确认文案「不可撤销」，误删只能翻文件历史；core `notifyUndo` 现成（favorites/review 在用）。onUndo 走 `addEntry` 式重插；加密条目销毁密文属真不可逆，保留二次确认。
2. **写日记弹窗加正文输入（价值高/成本中）**：闪念级记录现在必须「写日记→保存→跳编辑器」两跳；回忆墙头部 ✏️ 复用同一弹窗，受益面翻倍。方案：弹窗加可选 textarea，`saveNewEntry` 把内容传入 `addEntry`（该函数本就有 content 参数，目前恒传 `''`）。
3. **「那年今天」策划视角（价值高/成本中）**：日记的核心情感价值在回望。方案：日期筛选弹窗加「那年今天」按钮，`DateFilter` 扩展 `mode:'mmdd'`，按 `entry.date.slice(5) === 今天.slice(5)` 过滤；diary-wall 共用同一口径（见下）。
4. **「打开今天」直达命令（价值中/成本小）**：`bz-diary-today`，当日 md 存在则打开、不存在则创建后打开。
5. **写盘前新鲜度校验（价值中/成本中）**：编辑器正在写的日记与面板操作（writeFile 全量重写）之间存在覆盖窗口。方案：写前重读磁盘与 map 基线比对，不一致先 refreshFile 重放或提示冲突，不做静默覆盖。
6. **日期分隔条信息密度对齐 wall 节头（成本小）**：diary 日期分隔只有 `YYYY-MM-DD`，wall 节头有「周几+统计」。追加次级「周N · N条」。
7. **日期筛选弹窗：连续/缺口方块 + 「本月」快捷（成本中）**：按日计数的方块条（`--interactive-accent` 透明度分档，方形语言已拍板），navBar 加「本月」。

**已到位，避免误改**：搜索 ESC 两段式、标签增量切换与计数防抖、滚动置顶二分、「加密」标签解锁链、关闭面板即锁保险箱；头行 emoji 按钮虽与 lucide 不齐但该域冻结，不建议为此解冻。

## 2. diary-wall（回忆墙）

**现状**：原型 v5 一比一落地（真瀑布流+章节栏+灯箱/抽屉+右键委托），交互已到位。

1. **灯箱内上/下一篇导航（价值高/成本中）**：媒体墙核心动作是连看，现在「点开→关→再点」。方案：灯箱加左右按钮 + ArrowLeft/Right + 移动端滑动切图，切换前 pause 旧视频；此能力日后可下沉 core。
2. **章节栏年份分组（价值高/成本小）**：月名只渲染「N月」，跨年时「1月」出现两次分不清。方案：rail 按年份分组插年份分隔标签（`data-month` 已存完整 `YYYY-MM`，定位逻辑不动）。
3. **头行计数口径（成本极小）**：`renderAll` 的 range 恒为总数，筛选后头行计数不变——与其他域「头行计数=当前结果数」范式不一致。改 `filtered().length`。
4. **图标 lucide 化（成本小）**：头行 ✏️🔍✕、右键菜单符号、抽屉 📎 均为 emoji/文本符号——保留自绘菜单与容器委托（已拍板），仅图标源换 `uiIcon`。
5. **「那年今天」时光条（价值中-高/成本中）**：与 diary 第 3 条共用 mmdd 派生，wall 出媒体呈现（首屏顶部横滑条，不打断主瀑布流）。
6. **卡片/灯箱 cap 行去文件名（成本小）**：`IMG_2341.jpg` 对回忆无意义，改「emoji 时间 · 标签字」或直接去 cap。
7. **与日记本互跳（成本小-中）**：右键菜单/抽屉加「在日记本中查看」（带同筛选）；diary 头行加图标按钮反向开 wall。

**已到位，避免误改**：右键容器委托（拍板勿改回）、头行无设置按钮（拍板）、品牌文案「日记本」（ADR-0081 明文）、滚动条隐藏、未解锁加密不可见、ESC 分层。

## 3. attach（搬附件）

**现状**：命令→路径选择器（上次目录预高亮）→预览确认→`fileManager.renameFile`→toast 汇总，语义克制。

1. **撤销搬移（价值高/成本中）**：附件常被多篇笔记引用，搬错要逐个手动搬回。方案：runMove 成功后 `notifyUndo('已移动 N 个资源到 X', 逆序 renameFile 回原路径)`（链接自动回改）。
2. **选择器前置附件数（成本小）**：选完文件夹才看到「将移动 N 个」。先 `collectResources`，desc 改「当前笔记引用 N 个附件…」，0 个直接提示不弹选择器。
3. **大批量进度反馈（成本小）**：附件多时循环期间无反馈像卡死。≥10 个时用 core notice 的 progress 形态逐个更新「i/N」。
4. **预览升级为可勾选清单（成本中）**：现在只有数字，无法排除个别不想动的附件。自绘小弹窗逐行 from→to + 复选默认全选。

**已到位，避免误改**：同名冲突才改名、无手输路径（ADR-0061）、链接更新交 Obsidian 内建（ADR-0014 勿回退自研）。

---

# 三、保险库×剪藏组（encrypt / clipbook）

## 1. encrypt（统一保险库）

**现状**：ADR-0085 合并后三栏三资产骨架完整、安全感设计扎实（解锁节流、首设硬警告、60s 剪贴板清空）；主要缺口在**取密热路径步数**、**表单效率**、**健康/解锁状态可见性**与**残余 emoji**。

1. **快速取密路径（价值最高）**：最高频流程「解锁→查密码→复制」约 5 步，且解锁后落在「概览」展示页而非工作页。方案：小改——解锁成功后 `setAssetFromNav('pw')` 并聚焦搜索框（记住上次资产更佳）；中改——`bz-encrypt-copy-password` 命令弹轻量 fuzzy 选择器（复用 path-picker 范式），选中即复制，不解锁主面板也能走。成本小~中，价值高。
2. **密码表单效率（价值高/成本小）**：已有「生成」钮但没有强度提示、没有同平台+账号重复检测、输入框间无 Enter 流转。方案：密码框下加「弱/中/强」文字提示（纯本地计算）；保存前查重提示「该平台已有同名账号，再点一次放行」；Enter 依序 focus 下一字段、末字段 Enter=保存。
3. **误肩窥防护（价值中高/成本小）**：新增/编辑弹窗的密码框明文常驻；账号卡 👁 一旦点开永久明文。方案：表单密码框 `type="password"` + eye 切换；账号卡显示后 ~15s 自动回遮。
4. **健康状态「真实化」（价值中高/成本小）**：概览「保险库体检」卡传常量 `health: null` 永远显示「未体检」，左栏健康卡硬编码绿点——刚跑完体检回来两处依旧装没体检过。方案：UIManager 持有 `lastHealth`（体检完成写入、上锁清空），两处改读它并加可点直达。
5. **解锁会话可见性 + 安全模式空闲自动上锁（价值中/成本小-中）**：左栏「立即上锁」旁显示已解锁时长；安全模式下固定 15 分钟无交互自动上锁（面板内交互重置计时）——不加设置项。
6. **一致性收尾（成本小）**：预览窗关闭钮 `❌`、附件 chip 🎬🖼、收藏星 ★、全角＋、首设警告 ⚠️ 换两文件已有的 ICON_PATHS 内联 path。

**已到位（勿动）**：60s 剪贴板自动清空+降级复制、解锁指数节流、首设双确认、生成器安全随机、fav 常用置顶、favicon 头像、加密/还原确认+进度+原子中止、附件预览图随文走。旧 `src/password/` 经查能力已被全覆盖、命令已断，无体验残留，可按计划退役。

## 2. clipbook（剪藏本）

**现状**：三栏骨架与「列表零操作+中栏右键」范式落地干净；但**桌面搜索在融合时丢失**、**移动端没有长按抽屉**、**剪藏条目右栏读不到正文**是三个最真实的流程断点。

1. **桌面端搜索缺失（融合退步，价值最高/成本小）**：被并入的旧 clipping 域有「搜索+站点过滤+排序」，融合后头行只剩品牌+「聚合讯已接入」，`searchKw` 只能由移动端设置——桌面几百条只能肉眼扫。方案：desk-head 插搜索输入（`.bz-input` + search 图标），input 写 `searchKw` 后 `renderList()/renderRail()`（防抖 180ms 对齐保险库）。成本小，价值高。
2. **移动端条目无长按抽屉（两端不一致，价值高/成本小）**：移动端对条目只能点开详情/点头栏保存钮，「标记已读/查看原文/删除/复制链接」全部只有桌面右键才有。方案：`renderMobList` 对每卡调 `attachItemActions(card, buildItemActions(a), …)`——动作构建器与桌面同源，一处接入即全量对齐。成本小，价值高。
3. **剪藏条目在右栏读不到正文（价值高/成本中）**：剪藏扫描按契约只读 frontmatter，右栏选中剪藏=标题+摘要+一段空白，「阅读」第三栏对一半资产是空房间。方案：选中 `origin==='clip'` 时懒加载 `cachedRead` → 剥 frontmatter → `toParagraphs` 渲染，按 path 缓存、`clipping:file-modified` 失效；最小版先放「打开笔记」主按钮。成本中/小，价值高。
4. **未读流批量处理（价值中高/成本小-中）**：三天不打开就是上百条未读，逐条右键不现实。方案：rail 源行接 `attachItemActions`（rail 是导航层，不破坏「中栏列表零操作」拍板），动作「全部标为已读」带确认框写明 N 篇。
5. **误删/误标可撤销（价值中高/成本中）**：「标记为已读」即刻删 body、「删除」直接 `vault.delete` 连回收站都不经过。方案：删剪藏笔记改 `vault.trash`；已读/删除后 notice 带 ~5s 撤销（动作前留存 raw 副本，撤销走既有串行队列写回）。
6. **阅读动线（价值中/成本中）**：选中阅读后永远停在未读；标已读后选中跳回列表第一条而不是「下一篇」；长流阅读全程鼠标。方案：a) 复用阅读会话计时，停留超 ~10s 自动落「在读」（可手动覆盖）；b) 处理后前进到同位置下一篇；c) 右栏聚焦 ←/→ 或 j/k 切换。

**已到位（勿动/勿改坏）**：saved 三重保底去重、「列表零操作+中栏右键」、滚动条隐藏、桌面无关闭钮、B 站分流文献盒、阅读时长统计、保留策略、目录事件防抖增量刷新。桌面 frame 固定 1180×760 若要可缩放应走 `uiResizable`（todo 先例），属一致性选项非缺口。

---

# 四、书簇（bookshelf / library / reading-report）

## 1. bookshelf（书架墙）

**现状**：P1 原型一比一落地且过一轮审查修复——桌面左栏状态+统计行+常驻搜索+封面平铺，移动图标头行+筛选抽屉；详情弹窗、EPUB 只读、空态三态、加载态、坏图兜底、自动刷新均已到位。

1. **详情弹窗缺「打开原文 / 继续读」直达（价值高/成本小-中）**：书架墙核心回路是「看到书→打开书」，现在详情弹窗只有 删除/取消/保存：md 书回不到笔记、EPUB 回不到 Weave 阅读器。这是 library 有能力而 bookshelf 未覆盖的最大断档。方案：详情弹窗加主按钮「打开原文」（在读状态文案「继续读」）；EPUB 点击瞬间发 progress「正在打开…」；进阶用 `buildEpubJumpLink` 的 `path#weave-cfi=…` 深链跳当前位置（编码函数下沉 core，勿反向 import library）。统计行 accent 卡（正在读《X》）整卡可点一键回书。
2. **移动端没有排序入口（价值高/成本小）**：排序下拉只在桌面工具栏，手机永远「最近阅读」序。方案：头行补 arrow-up-down 图标钮，复用 `openFilterDrawer` 同构底部抽屉列 4 个排序键（或并入筛选抽屉一组 `uiSegmented`），对齐收藏本 P1 移动头行「＋→⇅→🔍→✕」秩序。
3. **状态保存缺撤销兜底（价值高/成本小）**：详情弹窗把「已读」手滑改「未读」再保存，readingDate/completionDate/进度全被清掉且 toast 只有「已保存」。方案：persistBook 前快照四键旧值，保存后 `notifyUndo`（core 现成 API）。
4. **左栏缺分类维度（价值中-高/成本小-中）**：数据层有 category 但没有浏览路径，书量过 50 后「只看某类」只能靠记得拼法去搜。方案：左栏「状态」组下加「分类」第二组（沿用现有样式，与状态正交过滤），移动端并入筛选抽屉。零新设置项。
5. **读书版「那年今天」：读完纪念日卡（价值中/成本中）**：「x 年前的今天你读完了《X》」，命中时统计行临时替换一张 accent 纪念卡，无命中不渲染零空态。
6. **报告→书架墙单向深链（价值中/成本中）**：报告里的作者 Top5/分类行点不回书架。方案：`openBookshelf` 支持带参预填 `M.searchKeyword`，报告作者/分类卡点击传作者名。

**已到位（勿动）**：按日期混排不分段（拍板）、在读侧默认最近在前、滚动条隐藏、关闭钮仅移动端、图标全 lucide、EPUB 进度回架自动刷新。星级评级属数据模型新增，建议等 library 退役后单独立票。

## 2. reading-report（读书报告）

**现状**：旧脚本逐字移植的 10 段长报告，分片渲染+进度 toast+错误人话化；但视觉仍是旧体系（内联样式、硬编码渐变、emoji、环形图）。

1. **长报告缺分节导航（价值高/成本小-中）**：10 段一滚到底。方案：面板顶部一行横向 chips（overflow-x:auto），每段容器加 id，点 chip `scrollIntoView`。
2. **三个环形图与已否口味冲突（价值中-高/成本中）**：时段/分类/互动分布都是 SVG donut——「圆形统计被否」已拍板（书架墙特意用柱状），且 donut 无数据标注。方案：改水平条形行，同文件 `sessionDistribution` 条形行就是现成范式。
3. **热力图锁死最近 1 个月（价值中-高/成本小-中）**：`processHeatmapData` 已算出全部月度数据，渲染却硬编码 `slice(0, 1)`。方案：段头加 ‹ › 月份切换（生成函数本就按月组织，直接复用）。
4. **年度维度无法下钻（价值中/成本中）**：年度统计只有逐年大卡。方案：年卡点击展开该年 12 月柱，与建议 3 共用一套月柱生成。
5. **头行/文案 emoji 与 lucide 铁律不符（成本小起）**：`🧮` 标题、`❌` 关闭、`🏆` 排名换 lucide；硬编码渐变卡的 token 收敛可另行一次不与图标绑定。

**已到位**：统计不卡死（先建窗+分片+让出主线程）、progress toast 唯一化、移动全屏跟随（拍板不设独立键）。

## 3. library（旧书库，待退役）

只报「不一致且 bookshelf 未覆盖」两条：

- **能力断档**：读书笔记浏览/双击跳原文/长按编辑删除是 library 独有、bookshelf 详情里「N 划 N 想」chips 不可点——退役前应把「读书笔记」入口迁入 bookshelf 详情弹窗（与 bookshelf 第 1 条同票处理），否则划线回顾能力随域蒸发；迁移时其「不可撤销」删除确认改走 `notifyUndo`。
- **设置键悬挂**：报告移动全屏键现挂 `libraryMobileDefaultFullscreen`，library 退役删除其设置页后此键将无 UI 入口——届时应随域切换到 `bookshelfMobileDefaultFullscreen`，属退役票迁移清单项。

---

# 五、收藏归物备忘待办组（favorites / belongings / memo / todo）

## 1. favorites（收藏本，P1 标签工作台）

**现状**：拍板形态落地完整（左标签栏、主头行、搜索+排序、右键浮层、移动抽屉、全 lucide）。

1. **批量打标（多选整理）**：标签工作台的收尾一环——改一条标签 4 步，新收 10 条重新归类要 40+ 步。方案：ctrl/cmd+点卡多选（shift 连选），底部浮出选择条「已选 N · 打标签（9 胶囊平铺）· 置顶 · 归档 · 删除 · 退出」；移动端不做。成本中，价值高。
2. **归档取回路径 + 归档撤销**：归档确认写「数据保留在 favorites.json」但全 UI 无任何入口看到或恢复归档条目，误归档只能手改 JSON；删除有 notifyUndo、归档没有——同一风险面双标。方案：a) 归档后 `notifyUndo`；b) 左栏「全部」下加「已归档」入口或搜索无结果时提示「归档中有 N 条匹配」。不破坏 ADR-0074 冷存拍板。成本 a 小/b 中，价值高。
3. **搜索覆盖 URL/域名 + 卡片域名师徽章**：收藏常见记忆点是域名（「那个 deepseek 的页面」），haystack 不含 url 搜空是常态。haystack 加 `it.url`；`host` 升级为 meta 行尾弱化小徽章。成本小，价值中高。
4. **添加表单贴链自动搬家（成本小）**：`#fz-title` 监听 paste，URL 形态内容移入 `#fz-url` 并回焦标题。

**已到位**：置顶品牌橙竖条+恒最前稳定分区、排序循环钮持久化、动作序与 ADR-0083 一致、ESC 双层+脏表单拦截。

## 2. belongings（归物本，P6）

**现状**：左四态栏、方形统计三卡（拍板）、年→月时间轴、状态流转 keepOpen、modify 自动刷新均已落地。

1. **状态流转/删除接 notifyUndo（容错主项，价值高/成本小）**：右键菜单里「标记为已丢弃」与「删除」相邻且即时生效；登记成本高的数据删错最痛。方案：流转成功后 `notifyUndo('「X」已标记为'+s, 回写旧状态)`；删除留 snapshot 撤销写回，确认弹窗保留、威慑文案去掉「不可撤销」。
2. **年节默认折叠策略（价值中/成本小）**：库用两年后从最新年一路铺到底。方案：当年/上一年展开、更早折叠（手动展开过的以会话内状态为准）。
3. **统计卡联动筛选（可选，价值中低/成本中）**：「总资产」卡可点=在用+闲置合成筛选，「在册件数」卡可点=回全部。不动拍板口径，只加「视图」。
4. **年份筛选与「平铺不下拉」口味的张力（成本小）**：年份 2~5 个却用原生 `<select>`。≤6 个时改平铺 chips，>6 保留下拉。

**已到位**：分类 picker 键盘/外点关、记一笔默认今天+使用中、桌面/移动同构、日均成本单一口径、转卖行「陪伴 N 天」语义降档。

## 3. memo（备忘录）

**现状**：旧脚本全量范式（列表弹窗+创建弹窗），功能都在但视觉交互仍是旧体系；域定位为待 todo 接管，**建议只做低成本容错与图标收尾，不投资新范式**。

1. **删除接 notifyUndo（价值高/成本小）**：最高频写入域，删除只有事前确认、事后无路。
2. **emoji 清换 lucide（成本小）**：头行 ✏️📁⚙️❌、卡内 📌/⚠️/📦、空态「🎉」是四域中唯一残留，属「不引语义变化的最小替换」。
3. **空态升级三件套（成本小）**：换 `uiEmpty`，「记第一条」直开创建弹窗；todo 空态同批换。

## 4. todo（待办，场景工作台）

**现状**：新范式完成度高（左场景栏、主头行、segmented 排序、底部 composer、行内勾选防抖、右键/长按统一、拖缩放+尺寸记忆、跨域同步）。

1. **删除接 notifyUndo（价值高/成本小）**：同 memo 同源数据，误删丢同一条。`TodoData` 增 restoreItem（或记 idx 撤销时插回原位）。
2. **场景管理闭环（价值中/成本小~中）**：左栏「添加场景」提示去 memo 设置手改逗号串，路径断裂；改名场景会让旧条目「消失」在原场景里。最小版：场景项右键加「在设置中编辑」直达；完整版：浮层加重命名（批量改 scene 字段+更新设置）与删除（非空确认迁入默认场景）。
3. **「重要」快速视图（价值中/成本小）**：重要是手动注意力标记，跨场景盘点要逐个点。方案：`sceneOptions()` 加伪场景「重要」（star 图标+警示色点），范式照「今日」伪场景现成先例。
4. **composer 录入后的补全半径（价值中/成本小）**：composer 恒 minor、无 due，「先记下来再补全」要右键→编辑两跳。方案：保存 toast 挂「补全」action 直开编辑器。
5. **已完成折叠条的时间界（价值低-中/成本小）**：几个月后「已完成 217 条」只有计数噪音。展开时默认只列近 30 天，尾部「更早 N 条」放全。

**已到位**：「今日」已覆盖跨场景今日聚合（不需要另做统计卡）、勾选防抖+反悔、排序与 memo 共键、移动端主头行让位 composer 的取舍。

### 跨域一致性小结（本组）

- 右键/长按浮层、keepOpen、companion、danger 红、动作序四域已统一走 `core/item-actions.ts`——已到位。
- **空态分化**是当前最明显缺口：favorites/belongings 用 `.bz-empty` 三件套，memo/todo 仍是单行灰字。
- **撤销范式分化**是第二缺口：notifyUndo 只有 favorites/review 在用，belongings/memo/todo 的删除与 belongings 的状态流转都是「事前威慑、事后无路」——三条小成本改动即可拉齐。

---

# 六、复习番茄启动器组（review / pomodoro / launcher）

## 1. review（复习计划）

**现状**：三区队列+整窗冲刺的拍板形态落地干净——冲刺无小字、答对 0.8s 自动跳、结果卡排期真实，简洁化方向执行到位不建议回退。剩余摩擦在键盘节奏、题库口径、普通复习模式的评级半径。

1. **冲刺答题键盘化（价值高/成本小-中）**：做题是高频纯键盘节奏场景；旧做题弹窗（quiz-core `_bindKeyboard`）有此能力，迁 SprintSession 时丢了。方案：`renderQuestion/renderResult` 挂 document keydown（输入焦点时跳过）：1-4/a-d 答题，Enter 依次尝试提交→下一题→结束并结算；会话 finish 时注销。
2. **答错后给一行解析（价值高/成本中-大）**：现在答错只标绿正确项，为什么错要自己回原文找——复习场景的核心信息缺口。方案：出题 prompt 增加 `explain` 字段（一句话解析+原文依据）随题存取；答错时选项下方渲染；存量题无 explain 静默不显示零迁移。
3. **普通复习模式的评级半径（价值高/成本中）**：`reviewLoop` 只开笔记+常驻通知，评级要回复习面板右键——流程在笔记页断头。方案：循环存续期间屏幕底部挂悬浮迷你评级条（忘了/困难/一般/简单+跳过，复用既有四档语义），点评级自动进入下一篇。
4. **三区列与「开始本轮」口径统一（价值中高/成本小-中）**：dueItems 含「R<阈值提前复习」篇目，但列表只看 nextReviewDate——用户看到的与做的不一致。方案：抽公共纯函数两处调用，R 提前到期的卡片挂「提前」tag 落「今天」列。信任队列的前提是数得准。
5. **冲刺中「跳过此篇」（价值中/成本小）**：某篇不想做只能放弃整轮。方案：头行加 skip-forward 钮，该篇回 pending 移到队尾，不评级不写盘。
6. **清零正反馈 + 结算屏连续天数（价值中/成本小）**：全清后条仍是红点、点了才弹「没有逾期笔记」；结算屏无打卡感。方案：清零时红点转绿+「今日已清空」+隐藏主按钮；结算屏追加「连续 N 天」统计块（computeStats 已算好 streak）。
7. **小项打包**：搜索框画了 `/` kbd 提示但无监听（补聚焦快捷键兑现 affordance）；R 值与排期口径统一改读拟合权重 `currentW()`；「累计复习 X 次」改「累计 X 天 · 连续 Y 天」（字段实为天数）；统计弹窗残留开发文案「对齐影视统计界面」应删；统计弹窗关闭钮 ❌ 与文件树徽标 ✅📅 换 lucide。

**已到位（勿再动）**：结果卡排期真实化、列头/底部去小字、答对自动跳、未通过「复习此笔记」唯一按钮+待重做 FIFO、抽屉右键/长按统一、撤销移出（notifyUndo）。统计弹窗浅色卡硬编码色是拍板的对齐影视样式，不翻案。

## 2. pomodoro（番茄钟）

**现状**：状态机纯函数化、不补算/后台自动暂停/强制专注语义扎实，状态栏+弹窗双承载；短板在弹窗外的操作半径和统计维度。

1. **阶段完成通知带动作按钮（价值高/成本小）**：autoCycle 关闭时专注完成只弹 toast，人若在别的笔记里得专程重开弹窗按「开始」——全域最高频断点。方案：`notifyPhaseComplete` 改 `notify(msg, { action: { label: '开始休息'/'开始专注', onClick: applyAction('start') } })`，core 现成能力（review「去复习」先例）。自动暂停/恢复维持不加 toast 的拍板不动。
2. **专注时段分布（价值中高/成本小-中）**：只有近 7 天计数柱，看不出「一天里什么时段在专注」。方案：7 天柱下加 12 槽（2 小时一格）小方柱（条形/方形语言，非圆形）；顺带 7 天柱 title 扩「N 个 · M 分钟」。
3. **今日行补总时长（成本小）**：「今日 N 个 🍅」在混用预设时不含信息量；`HistoryEntry.duration` 已存秒数没用上。改「今日 N 个 · M 分钟」。
4. **本轮循环位置可视（价值低-中/成本小）**：ring 下方渲染 N 个 6px 方点（已完成填 accent），可替代「专注 2/4」文字小字——少一处小字符合简洁方向。
5. **小项打包**：🍅 换 lucide timer；`#pomodoro-mask` 写死 rgba 对齐 `--background-modifier-cover`；可选 Space 切换开始/暂停。

**已到位（勿动）**：暂停态状态栏醒目标识、skip 静默语义、强制专注按钮锁定、提示音三阶段、恢复方式、桌面 hover 幽灵设置钮。

## 3. launcher（启动器）

**现状**：网格磁贴+长按编辑+安卓式拖拽推挤形态完整；CONTEXT 已拍板「与 home 并存不互改、后续可删」——只值得小成本修补。

1. **幽灵磁贴的一键清理路径（价值中高/成本小）**：命令被删后磁贴灰掉，点击只弹「命令不存在」，用户得知道「长按→编辑→点磁贴→删除」四步。方案：ghost 分支改为进编辑模式直接 `openTileMenu(tile.id)`，或 notice 挂「去清理」action。
2. **编辑模式可发现性（价值中/成本小）**：有磁贴后无任何编辑入口线索（0.5s 长按不可见）。方案：桌面 hover 浮现 pencil 幽灵钮进编辑（仿番茄钟 `pomodoro-settings-hidden`），移动端维持长按；桌面磁贴右键直接开磁贴菜单。
3. **命令选择器 ↑↓ 键盘导航（价值中/成本小-中）**：已有搜索+Enter 取首命中，列表百余条时只能鼠标挑。补 ↑↓ 高亮 + scrollIntoView，Enter 选高亮项。
4. **编辑控件去文本符号（成本小）**：「文」字符钮换 lucide `type`、「✓ 完成」换 `check`+文案；picker 遮罩 rgba 对齐 token。

**已到位（勿动）**：长按 press-active 压馈、pointercancel 回弹、磁贴尺寸菜单化、空态引导、手势默认关。
