# 320 挂载树·列表侧四项与全包门禁

- 状态：已交付（卡片列表行引用计数徽标（只数用户双链，0 不显示）+ 孤儿筛选开关与行标记 + 整库扫描每轮 refresh 一次并缓存到面板对象；`src/knowledge/ui.ts`／`styles.css`，`tests/knowledge/mount-list.test.ts` 4 用例。反向视图/面包屑等画布四项已由 317 覆盖）
- 关联：ADR-0138 §5（已采纳路线图，⑥布局持久化已作废）／ spec §界面 ／ `src/knowledge/ui.ts`（列表渲染）／ AGENTS.md 质量门禁
- 依赖：315（反向索引）；317（面包屑与反向视图若未收口则在此补齐）

## 交付

- [ ] 卡片行**引用计数徽标**（只数用户双链：正文 wikilink + related + mounted）。
- [ ] **孤儿卡**筛选 + 行标记（既无入链也无挂载）。
- [ ] 反向视图与面包屑若 317 未收口：在此补齐（含顶栏方向文案与测试）。
- [ ] 全包门禁：`pnpm test`（含 smoke）＋ `tsc --noEmit` ＋ 自审 ＋ diff 审查 ＋ 构建验证；样式聚合进根 `styles.css`。
- [ ] 文档回写：`HANDOFF.md` 状态段（原型 → 已实现）、`CONTEXT.md` 词条复核（挂载树 ／ 建议否决）、ADR 索引。

## 验收

全绿；卡片列表能用徽标与「孤儿」筛选回答「哪些卡没人挂、哪些卡没挂过谁」。
