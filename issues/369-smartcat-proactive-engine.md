# 369 · 实现：小橘主动话题引擎（49 + 62 第一步，351 拍板完成）

> labels: wayfinder:task ｜ map: 346 ｜ status: open ｜ assignee: — ｜ blocked-by: —
> 上游：351 拍板票（已闭，2026-09-17 用户「全按推荐」）

## 拍板记录（六条，全按推荐）

1. **架构 = 统一「主动话题引擎」**：域动静做成现有 Bandit 选臂的新话题臂，不另起播报通道。时机挑选（节律+安静期门控）与话题学习（Bandit 反馈）沿用既有机制，新增的只是「臂池素材 = 用户真实生活动静」。
2. **首版素材池**（行为流「产出类」事件，噪音不进池）：读完一本书（书库「讫」印）/ 复习里程碑（到期清零、连续打卡 N 天）/ 番茄累计（本月第 N 个、百小时整数关）/ 日记连续 streak（3/7/30 天节点）/ 剪藏阅读里程碑（本周读了 N 篇）。
3. **频率不加量**：沿用现有节律（每周 1-2 次主动关心 + 每日一次温和问候上限 + 安静期门控优先），域动静只丰富话题内容。
4. **话术 = 邀请话题**，走小橘人格 + 当前 PAD 心情调制（同一件事不同心情说法不同），非系统通知腔；拿不准宁可不说。
5. **开关**：设置一个总开关（默认开），关闭回落纯陪伴话题模式。
6. **62 后续路线**（记 fog，49 落地后开票）：月度对话 → 年度故事（与 next-ideas #63「年度之书」合并候选）。

## 实现口径

- 消费侧：行为流（smartcat-behavior.json）新增「产出类事件识别器」（纯函数，里程碑阈值口径进测试）；识别结果入 Bandit 臂池（routing.ts 现有选臂机制扩展，不重构）。
- 呈现：现有气泡通道（bubble.ts），行为流照记（小橘行为流自身语义不变）。
- 防打扰：quiet-gate / 每日上限 / 总开关三层既有结构不动，只接素材。
- 设置键：smartcat 域 schema 加总开关（键名如 smartcatProactiveLife，默认 true）。
- 门禁：数据层（识别器/选臂扩展）+ UI 层 + smoke 同步；门禁全绿。

## Resolution

（完成后填写）
