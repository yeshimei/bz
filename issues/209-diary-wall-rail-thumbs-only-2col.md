# 209 — 回忆墙：章节栏纯缩略图化 + 正文双列 + 稀疏挤压修复

用户拍板（2026-09-06，原型 `rail.html` 十选一 + 多轮布局评审收束）：墙保持「之前的那个」（日期节头 + 分节瀑布），
章节栏选 R1 结构做减法，正文改双列。

## 拍板项

1. **章节栏纯缩略图**（R1 减文字）：删掉栏内全部文字——「章 节」标题、年份分组标签、月份名、条数；
   每月 = 一簇缩略图（月内前 6 个含图/视频条目的首个非音频媒体，3 列 × 2 行）。
   音频无缩略图 → 跳过；纯文字月无缩略图 → 不渲染该月簇（无法从栏内定位，接受）。
2. **章节栏缩略图含视频**：视频缩略 = `<video muted preload="none" data-src>`，
   章节栏自己的 IntersectionObserver 进视口才升 `preload="metadata"` 读首帧，离视口暂停（修「开墙全量解码」卡顿）；
   图片缩略 = `loading="lazy" + decoding="async"`。加载失败回退 image 图标（沿用）。
3. **正文双列**：桌面 `.bz-diary-wall-masonry` column-count 3 → 2（移动端本就 2，不动）。
4. **稀疏挤压修复**（图1 病根）：删 `--sparse-2` 的 `width:50% + inline-block` 半宽 hack
   （在多列容器里百分比按列宽解析 → 卡片压成细条）；`--sparse-1` 文字条跨列保留。

## 验收

- 章节栏无任何文字节点（标题/年份/月名/条数全无），只有缩略图簇；点簇跳月、滚动高亮（.on）不变。
- 视频缩略进视口才加载首帧；离视口暂停；jsdom 无 IO 时立即挂载（测试确定性）。
- 桌面瀑布两列；单条日文字条仍跨列（sparse-1）；不再产生 sparse-2 半宽内联块。
- 全量 pnpm test + tsc --noEmit 绿。
