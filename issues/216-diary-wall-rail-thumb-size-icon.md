# issue 216：章节栏胶卷格去多余图标 + 固定尺寸

## 病根（用户截图，issue 212 引入）
1. 每格都先塞「图片占位图标」再挂 img——图加载出来图标也不消失（每格左侧多破图小标）；
2. 格子 `flex: 1 1 0` 均分行宽——缩略少的月份每格被拉成整行宽条。

## 修复
- thumbEl：图片格不再预置图标（error 回退时才出现）；视频格保留播放角标浮层；无资源才纯图标。
- CSS：格子 `flex: 0 0 auto` 固定 20×20，不再伸缩。
