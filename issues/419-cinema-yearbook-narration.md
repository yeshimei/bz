# 419 · cinema yearbook 年度旁白

- 状态：待实现（2026-09-23 用户拍板采纳）
- 来源：`.scratch/jev-llm-智能化切入点清单.md` 第 34 号（第三梯队）
- 关联：ADR-0175（cinema 分析面板交互先例）
- 号位说明：418 之后取 419

## 现状

`yearbook/` 26 幕观影分析长片，`yearbook/data.ts deriveYb` 纯 frontmatter 派生——全是数字与图，无一句叙事。

## 方案

首幕/落幕加 LLM 年度旁白：当年观影数据（`deriveYb` 产物）→ 一段总结文案。**当年一次、缓存复用**（缓存键 = 年份 + 条目签名/哈希，数据变化才重算）；LLM 不可用/失败 → 静默降级为现状纯数据幕，不阻断长片渲染。

## 落地

- [ ] 旁白生成 + 按年缓存（失效键含条目签名）；
- [ ] 失败/未配置静默降级。

## 测试

缓存命中零调用、签名变化重算、降级路径。
