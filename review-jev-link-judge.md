# Jev 作为自动关联裁判 · 实测报告

日期:2026-09-21 | 数据源:真实 vault「叫我包仔」三盒 61 篇 | 模型:jev-1.13.0

## 结论

**技术上可用,中文不是短板 —— 但换它的代价是「判定口径」会变。**

Jev 比现有裁判(core/ai → DeepSeek)**更硬**:明确关系判得极准,模糊软关联会被判成不相关。
所以这不是"替换一个函数",而是"换一套判定标准",阈值要重校、弱链会掉。

## 验证方法

完全复刻 `link-agent/pipeline.ts` 的 `JUDGE_PROMPT_PREFIX` 口径与 `dossierCard` 档案卡形态:

- **新笔记** = 已有关联的笔记(正文首块 400 字)
- **正样本** = 双向关联网(**自己的 related ∪ 反向链**,单侧写入下必须双向取)中实际存在的
- **负样本** = 同 domain 的其他笔记 —— 这才是真实向量召回的难度
- **每题设计**:每候选 1 个 Noul(是否实质关联)+ 1 个 Score(关联强度 4 档),一次调用全问完

## 判定质量

| 指标 | 结果 |
|---|---|
| 硬关系(历史人物/事件) | Noul **0.96–0.99** — 极准 |
| 软关联(心理随笔弱连接) | Noul **0.29–0.39** — 比现有裁判保守,判不出来 |
| 同领域干扰项误建 | **2/27(7%)**,均为「严嵩」这类时代共现 |
| Score 分离度 | 正均值 **2.48** / 负均值 **0.84** — 比 Noul 更干净 |
| 阈值 0.65 表现 | 召回 6/9(67%)｜误建 2/27(7%) |
| 实测最优阈值 | ≈**0.72** |

典型样本(历史域):海瑞→治安疏 0.99 / 海瑞→嘉靖 0.96 / 治安疏→嘉靖 0.98 —— 全对。

## 工程指标

| 项 | 数值 |
|---|---|
| 单次调用 | 8–10 候选 + 16–20 问题,input 2600–4800 token |
| 延迟 | **1–2 秒**(含跨国网络;官方标称 70–500ms) |
| 成本 | ≈ **$0.00017 / 篇**(4000 tok × $0.042/M,output 免费) |
| 解析 | **零** — 返回即类型化值,不存在 JSON 解析失败 |
| $5 赠金 | ≈ **3 万篇**(30 天过期) |

对比现状:每篇 1 次生成式 LLM 调用 → 输出 JSON 文本 → 解析 → 失败入队重试。
Jev 消掉了「生成 + 解析」两层。

## 三个发现

1. **中文可用。** 上一轮的担心不成立 —— 它对中文人物关系、历史事件的判断相当准。
2. **口径差异是真问题。** 它只认明确关系(同一事件/人物/直接引用),不认「同属一个宽泛领域」。
   现有裁判建的弱链,它多半判 0.3 左右。这决定的是关联密度,不是对错。
3. **`reason` 字段该删了。** 现有 prompt 要求 LLM 生成「一句话理由」,而 `pipeline.ts` 只取 `path`,
   reason 从没写进 related、也没有 UI 消费 —— **白花的输出 token**。Jev 不给理由,恰好少给的正是你扔掉的。

## 建议:做成可插拔,而不是替换

```
LinkJudge (接口)
├── CoreAiJudge  ← 默认,现有逻辑原样
└── JevJudge     ← 填了 TypeSafe key 才启用
```

- Noul 判「是否关联」、Score 判「强度」,组合成三段路由:
  **高置信 → 自动建链｜中段 → 待确认(复用现有队列基建)｜低段 → 丢弃**
- 阈值**必须用真实库校准**(实测 0.65 偏松,0.72 更优;样本仅 5 篇,需扩样)
- 既有设置项 `linkAgentMinScore` 的语义要重新定义

## 附带发现(与 Jev 无关,现有管线的问题)

| 问题 | 数量 |
|---|---|
| related 真死链(目标文件不存在) | **21 条**,涉及 17 篇 |
| related 指向盒外(如 `归档/网页剪藏/*`) | **10 条**,违反 ADR-0141 §2 |
| 盒内有效链接 | 62 条 |

死链本该被 `cleanDeadLinks`(`linkAgentAutoClean` 默认开)清掉却没清,值得单独排查。
盒外链接属 ADR 生效前的旧数据,现有清理不覆盖这种情形(目标存在、只是不在盒内)。

另:这轮评测我一开始把「单侧写入的 related」当成关联全集,导致误报 22% 误建 ——
**单侧写入意味着关联天然不对称**,统计/查询/审计时都要双向取链,别踩同一个坑。

## 复现

```
.scratch/jev-probe/
├── judge_eval.py        # 单篇 + 随机干扰项(第一轮)
├── judge_batch2.py      # 批量 + 同领域难负样本(修正版,主结果)
├── count_anomalies.py   # related 数据异常盘点
└── batch_fixed.json     # 逐候选明细
```

`.env` 内含 key,`.scratch/` 已在 .gitignore 内。**建议用完后轮换该 key**(它已出现在对话记录里)。
